/* 贴底主行动按钮（2026-08-06）。
   设计稿里 Token 管理 / 确认购买 / 图片详情三页底部是同一枚按钮，底图也是同一张
   （assets/ai/图标/Group19444483.png → assets/images/primary-btn-bg.png）。

   单独成文件、由 app.wxss 统一 @import 一次，理由同 styles/fold-adapt.wxss：
   ① 三个页面分属 gallery / token 两个**不同分包**，分包之间不能互相引用，
      从一个分包 @import 另一个分包的 wxss 不可取；
   ② 放主包一份，两个分包都能用，也不必各自复制一遍。

   与 subpackages/settings/shared.wxss 的 `.primary-btn` 的区别：那枚是纯 CSS 渐变，
   这枚用设计稿给的磨砂底图（带外发光），两者并存，不要互相替换。 */

/* ⚠️ `z-index: 2` 不可省：`.fold-scroll` 是 z-index:1 的透明层，从导航栏一直铺到根容器底部，
      正好压在这枚贴底按钮上；层级缺省时按钮「看得见、点不动」
      （2026-08-05 设置页退出登录/用户注销的线上故障，说明见 styles/fold-adapt.wxss）。

   底图 primary-btn-bg.png 是 726×184 的整图，实心胶囊只占其中 654×112
   （实测 alpha 边界 x[36,689] y[30,141]），四周是外发光。 */
.cta-button {
  position: absolute;
  right: 48rpx;
  bottom: calc(60rpx + var(--safe-bottom, 0px));
  left: 48rpx;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 112rpx;
  color: #ffffff;
  font-size: 34rpx;
  font-weight: 600;
  line-height: 1;
}

/* 主按钮底图层（2026-08-21 起全站主按钮共用）。
   ⚠️ 小程序的 WXSS **不能用 `url()` 引本地图片**，所以底图只能是按钮里的一个 `<image>` 子节点：
       <view class="某某按钮"><image class="cta-button-bg" src="/assets/images/primary-btn-bg.png" mode="scaleToFill" />文案</view>
   父节点必须是定位元素（`position: relative/absolute`），否则百分比与负偏移会算到更外层的盒子上。

   尺寸全用百分比，于是**任意尺寸的按钮都能直接套**，不必每个按钮各算一遍 rpx：
     宽 726/654 = 111.009%      左 -36/654 = -5.505%
     高 184/112 = 164.286%      上 -30/112 = -26.786%
   （百分比的 top 按父盒**高**算、left 按父盒**宽**算，正是这里要的。）
   代入后胶囊实体恰好铺满按钮盒本身，外发光溢到盒外——按钮多宽多高都对得上，
   胶囊的圆角也跟着按钮高度等比缩放，不会被拉成椭圆。
   原来这里写的是固定的 726×184rpx + -36/-30rpx，只有 654×112rpx 那一种按钮能用。 */
.cta-button-bg {
  position: absolute;
  top: -26.786%;
  left: -5.505%;
  z-index: -1;
  width: 111.009%;
  height: 164.286%;
}

.cta-button--disabled {
  opacity: 0.55;
}

/* 贴底按钮是 `position: absolute` 且留在滚动区**外**，滚动内容必须把它占的高度让出来，
   否则变长的内容会从按钮底下穿过去（同 settings/shared.wxss 的 .settings-scroll-body）。
   196rpx = 按钮高 112rpx + bottom 60rpx + 24rpx 余量。 */
.cta-button-gap {
  padding-bottom: calc(196rpx + var(--safe-bottom, 0px));
}
