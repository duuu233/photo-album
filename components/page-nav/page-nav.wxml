<view
  class="page-nav custom-class {{immersive ? 'page-nav--immersive' : ''}} {{compact ? 'page-nav--compact' : ''}}"
  style="--nav-status: {{statusBarHeight}}px; --nav-height: {{navHeight}}px; --nav-row-top: {{rowTop}}px; --nav-row-height: {{rowHeight}}px; --nav-capsule-space: {{capsuleSpace}}px; --nav-center-max: {{centerMaxWidth}}px;"
>
  <view class="nav-row">
    <view wx:if="{{showBack}}" class="nav-btn nav-back" hover-class="nav-btn--hover" aria-label="返回" bindtap="handleBack">
      <image class="nav-back-image" src="/assets/images/ai-back-button.png" mode="aspectFit"></image>
    </view>
    <view wx:else class="nav-btn nav-placeholder"></view>

    <!-- 标题位（2026-08-01）：传了 title 就是普通标题；不传则把这块让给默认 slot，
         供「设备照片 / 投屏管理」把设备选择下拉框放进导航栏（产品要求去掉页面标题）。
         .nav-title 关掉了 pointer-events（避免吃掉下方内容触摸），slot 容器必须打开，
         否则下拉框点不动。 -->
    <view class="nav-title" wx:if="{{title}}">{{title}}</view>
    <view wx:else class="nav-center"><slot></slot></view>

    <!-- 右侧入口：传 rightIcon 就画图标（图源自带留白，按 .nav-right-image 的几何补偿摆正），
         否则退回 rightText 文字；两者都没有时占位撑住 flex 布局，标题才不会被挤偏。 -->
    <view wx:if="{{rightIcon}}" class="nav-right nav-right--image" hover-class="nav-btn--hover" bindtap="handleRight">
      <image class="nav-right-image" src="{{rightIcon}}" mode="aspectFit"></image>
    </view>
    <view wx:elif="{{rightText}}" class="nav-right {{rightText === '+' ? 'nav-right--icon' : ''}}" hover-class="nav-btn--hover" bindtap="handleRight">{{rightText}}</view>
    <view wx:else class="nav-right nav-placeholder"></view>
  </view>
</view>
