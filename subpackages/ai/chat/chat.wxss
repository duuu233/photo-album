.ai-root {
  position: relative;
  isolation: isolate;
  display: flex;
  flex-direction: column;
  height: 100vh;
  /* 原来这里留 166rpx 给底部 tabbar。tabbar 在 AI 页已移除（2026-08-10 需求 5），
     内边距一并收掉，输入卡片就顺势下移贴到安全区 —— 需求「把当前操作对话框往下移」。 */
  padding-bottom: var(--safe-bottom);
  overflow: hidden;
  color: #292929;
  background: #f8f5ef;
  box-sizing: border-box;
}

.ai-page-background {
  position: fixed;
  inset: 0;
  z-index: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

/* ==================== 顶部导航 ==================== */
.ai-header {
  position: relative;
  z-index: 2;
  flex: 0 0 auto;
  padding-top: var(--status-bar);
}

.ai-header__row {
  position: relative;
  height: var(--navigation-height);
}

/* 左上角按钮组：[返回][会话列表] 并排（2026-08-10 需求 5）。
   两张图标 PNG 都是 136×136、几何完全一致（与 components/page-nav 的返回键同源）：
   「看得见的白色圆角方块」只占画布 48.5%，四周是透明投影留白 ——
     · 图放到 64px，白块才 31px，与右侧微信原生胶囊(32px)等高；
     · 点击盒收到 44px 宽，两颗按钮的白块间距才是 44−31=13px（盒子 64px 宽会拉开到 33px，散架）；
       溢出的图 pointer-events:none，只由 44px 的盒子收点击，免得吃掉聊天区顶部的触摸；
     · 组左边距 13px → 第一个白块左边缘落在 13+(44−31)/2 ≈ 20px = 聊天区左内边距上；
     · 投影往下糊，白块在画布里偏上 5.15%，所以图再下推 5.15% 自身高度，
       让「白块」而不是「图+投影」上下居中。*/
.header-buttons {
  position: absolute;
  top: 50%;
  left: 13px;
  display: flex;
  align-items: center;
  transform: translateY(-50%);
}

.header-button {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
}

.header-button__image {
  flex: 0 0 auto;
  width: 64px;
  height: 64px;
  transform: translateY(5.15%);
  pointer-events: none;
}

.header-button--pressed,
.quick-tool--pressed {
  opacity: 0.68;
}

/* 标题**屏幕正中**（2026-08-10 需求 1.3）：left/right 双 0 + margin auto 才是真居中，
   靠「right: 某个宽度」去挤只会把中心点推偏（同 components/page-nav 的 .nav-center 注释）。
   max-width 定死 250rpx(125px)：左边按钮组到 101px、右边微信原生胶囊从 ~281px 起，
   居中元素两侧等距，125px 宽时占 [125,250]，两头都不碰 —— 需求「固定标题长度 超出隐藏」。
   pointer-events 关掉：这块盖在按钮组上方，开着会让返回/会话按钮点不动。*/
.ai-header__title {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  max-width: 250rpx;
  margin: 0 auto;
  overflow: hidden;
  color: #111111;
  font-size: 34rpx;
  font-weight: 700;
  /* 行高＝导航行高，就地垂直居中。⚠️ 不能改回 display:flex + align-items 居中：
     flex 容器里的裸文本是匿名 flex item，text-overflow 对它不生效，超长标题会被硬裁没有省略号。*/
  line-height: var(--navigation-height);
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
  pointer-events: none;
}

/* ==================== 聊天区 ==================== */
.chat-wrap {
  position: relative;
  z-index: 1;
  display: flex;
  flex: 1;
  flex-direction: column;
  min-height: 0;
}

.chat-scroll {
  flex: 1;
  min-height: 0;
  padding: 0 40rpx;
  box-sizing: border-box;
}

.chat-scroll--warming {
  opacity: 0;
}

::-webkit-scrollbar {
  display: none;
  width: 0;
  height: 0;
  color: transparent;
  background: transparent;
}

.chat-loading {
  position: absolute;
  inset: 0;
  min-height: 0;
  background: rgba(248, 245, 239, 0.78);
}

.welcome {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 84rpx 36rpx 30rpx;
  text-align: center;
}

.welcome-mark {
  width: 210rpx;
  height: 210rpx;
}

.welcome-mark image {
  width: 100%;
  height: 100%;
}

.welcome-title {
  display: flex;
  align-items: center;
  gap: 9rpx;
  margin-top: 14rpx;
  color: #5a5a5a;
  font-size: 34rpx;
  font-weight: 500;
}

.welcome-title image {
  width: 30rpx;
  height: 30rpx;
}

.welcome-text {
  margin-top: 10rpx;
  color: #5f5f5f;
  font-size: 27rpx;
  line-height: 1.5;
}

.welcome-suggestions {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 15rpx;
  margin-top: 32rpx;
}

.welcome-suggestion {
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-height: 56rpx;
  padding: 0 22rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.88);
  border-radius: 999rpx;
  color: #656565;
  background: rgba(255, 255, 255, 0.72);
  box-shadow: 0 10rpx 26rpx rgba(84, 70, 51, 0.07);
  font-size: 25rpx;
}

.welcome-suggestion image {
  width: 28rpx;
  height: 28rpx;
}

.welcome-suggestion:active {
  transform: scale(0.98);
  opacity: 0.76;
}

.message-time {
  margin: 26rpx 0 18rpx;
  color: #aaa6a1;
  font-size: 25rpx;
  text-align: center;
}

.msg-row {
  display: flex;
  align-items: flex-start;
  margin: 20rpx 0;
}

.msg-row--user {
  flex-direction: row-reverse;
}

.bubble {
  max-width: 560rpx;
  padding: 20rpx 24rpx;
  border-radius: 28rpx;
  font-size: 29rpx;
  line-height: 1.55;
  word-break: break-all;
  box-sizing: border-box;
}

.bubble-text {
  display: block;
}

.bubble--ai {
  width: 100%;
  max-width: 100%;
  color: #555555;
  /* border: 2rpx solid rgba(255, 255, 255, 0.9); */
  background: rgba(255, 255, 255, 0.78);
  box-shadow: 0 12rpx 30rpx rgba(89, 76, 58, 0.08);
}

.bubble--ai.bubble--loading {
  width: auto;
  padding: 0;
  overflow: visible;
  background: transparent;
  box-shadow: none;
}

/* 等待气泡（2026-08-12 需求 5：三个点改成动态 loading）。
   ⚠️ 胶囊与阴影要与原来那张 assets/images/ai-loading-bubble.png **看着一样**，这几个数就是
   从原图量出来的：画布 213×151，可见胶囊 x31~180 / y25~112（150×88，两端全圆），
   填充 #fbf9f4，阴影是往下糊的暖褐色（画布底部留白比顶部多 14px）。
   原来按 width:142rpx 渲染，换算后胶囊约 100×59rpx —— 下面就照这个尺寸画。
   原图整体带约 21rpx 的透明左边距，改成 CSS 后胶囊左缘与其它 AI 气泡的背景左缘对齐了，
   回复到达时不再有一次横向微跳。 */
.loading-bubble {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  width: 100rpx;
  height: 60rpx;
  background: #fbf9f4;
  border-radius: 999rpx;
  box-shadow: 0 10rpx 22rpx rgba(64, 42, 21, 0.1);
}

/* 三个点依次亮起+轻跳，一轮 1.2s。静止态压到 0.28 透明度（原图最左那个点也是淡的），
   所以即使某些机型不跑动画，看到的仍是「三个深浅不一的点」，与原图接近，不会变成三个死点。 */
.loading-bubble__dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #ff7a3d;
  opacity: 0.28;
  animation: loading-dot 1.2s ease-in-out infinite;
}

.loading-bubble__dot:nth-child(2) {
  animation-delay: 0.18s;
}

.loading-bubble__dot:nth-child(3) {
  animation-delay: 0.36s;
}

@keyframes loading-dot {
  0%,
  70%,
  100% {
    opacity: 0.28;
    transform: translateY(0) scale(0.86);
  }
  35% {
    opacity: 1;
    transform: translateY(-6rpx) scale(1);
  }
}

.bubble--user {
  color: #555555;
  background: #ffe0cd;
  border-top-right-radius: 10rpx;
}

.bubble--image {
  width: 430rpx;
  padding: 10rpx;
  background: rgba(255, 255, 255, 0.86);
  box-shadow: 0 12rpx 30rpx rgba(89, 76, 58, 0.07);
}

/* ⚠️ 宽度必须定在这一层，不能定在 .img-box 上。
   .img-box 靠 `padding-bottom: pad%` 占高度，而**百分比 padding 是按父元素宽度算的**，
   不是按自己的宽度。原先 .bubble-imgs 不设宽（撑满气泡内容区 622rpx）、.img-box 定宽 510rpx，
   于是竖图占位高 = 622×133.33% = 829rpx 配 510rpx 宽 → 盒子实际比例 162.6%，比图片(133.33%)高一截，
   aspectFill 为了填满就按高度放大、左右各裁掉 56rpx（图右边看着没显示全，2026-08-06 反馈）。
   父子同宽百分比才算得对：宽度放这儿（与 .gen-canvas-wrap 并在下方同一条规则里写），
   .img-box 用 100% 跟着走。.bubble--image（用户发的图）本来就是父子同宽，不受影响。*/
.bubble-imgs {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}


.img-box {
  position: relative;
  width: 400rpx;
  max-width: 100%;
  height: 0;
  overflow: hidden;
  border-radius: 22rpx;
  background: #eee9e2;
}

.bubble-imgs .img-box {
  width: 100%;
}

/* 用户发的图气泡宽 430rpx、内边距 10rpx → 内容区 410rpx，而 .img-box 默认 400rpx，
   图会靠左、右边多留 10rpx 白边。这里让它铺满内容区，四周留白才对称。*/
.bubble--image .img-box {
  width: 100%;
}

.img-el {
  position: absolute;
  inset: 0;
  display: block;
  width: 100%;
  height: 100%;
}

.caret {
  color: #ef641e;
  animation: blink 0.8s infinite;
}

/* —— 流式回复（2026-08-06 SSE）：预描述 + 生成进度条 ——
   两块都长在 AI 气泡内部、正文与图片上方，所以气泡宽度/背景一律沿用 .bubble--ai，
   这里只管自己的内边距与间距，别再各自开一个卡片。*/
.pre-text {
  display: flex;
  align-items: flex-start;
  gap: 10rpx;
  padding: 14rpx 18rpx;
  border-radius: 18rpx;
  background: rgba(239, 100, 30, 0.06);
}

.pre-text__icon {
  flex: none;
  width: 28rpx;
  height: 28rpx;
  margin-top: 6rpx;
}

.pre-text__body {
  flex: 1;
  color: #8a6a52;
  font-size: 26rpx;
  line-height: 1.5;
}

/* 气泡内的纵向间距。2026-08-07 起顺序是 预描述 → 占位盒/真图 → 正文
   （正文改到图片下方），这几条相邻规则要跟着这个顺序走，别照旧写 .bubble-text + .bubble-imgs。*/
.pre-text + .bubble-text,
.pre-text + .gen-canvas-wrap,
.pre-text + .bubble-imgs,
.gen-canvas-wrap + .bubble-text,
.bubble-imgs + .bubble-text {
  margin-top: 18rpx;
}

/* —— 生成中的图片占位（2026-08-07）——
   出图要几十秒，原先只有一条细进度条，用户盯着一片空白等。改成**一整块**跟成图同尺寸的
   渐变占位：进度百分比摆正中间，100% 时原地换成真图。
   尺寸/圆角必须与 .img-box 完全一致，换图那一下才不会跳，所以宽度与 .bubble-imgs 并在一处写。*/
.bubble-imgs,
.gen-canvas-wrap {
  width: 510rpx;
  max-width: 100%;
}

/* 进度文案压在百分比正下方、盒子**内部**（2026-08-07 需求，原先是盒外上方的一行灰字）。
   压在渐变上所以用半透明白：与 __value 拉开层级，一眼分得出「47%」是主、文案是注解。*/
.gen-canvas__label {
  color: rgba(255, 255, 255, 0.88);
  font-size: 24rpx;
  letter-spacing: 0.5rpx;
  text-shadow: 0 2rpx 8rpx rgba(120, 92, 74, 0.22);
}

/* 与 .img-box 同一套占位手法：height:0 + padding-bottom 百分比。
   百分比按父元素宽度算，所以宽度定在 .gen-canvas-wrap 上、这里必须 100%
   （踩坑记录见 .bubble-imgs 上方注释与 tests/ai-image-aspect.test.js）。*/
.gen-canvas {
  position: relative;
  width: 100%;
  height: 0;
  overflow: hidden;
  border-radius: 22rpx;
  /* 底色取项目里那套暖调（橙 #ef641e / 用户气泡 #ffe0cd）的柔化版，压住饱和度别抢成图的风头 */
  background:
    radial-gradient(120% 90% at 6% 0%, #f6b6d8 0%, rgba(246, 182, 216, 0) 58%),
    radial-gradient(110% 90% at 100% 6%, #f9d5ac 0%, rgba(249, 213, 172, 0) 62%),
    radial-gradient(130% 110% at 100% 100%, #b9c7ee 0%, rgba(185, 199, 238, 0) 64%),
    linear-gradient(158deg, #f0c2e0 0%, #e3c6e4 46%, #c8bce9 100%);
}

.gen-canvas__inner {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  /* 图标 / 百分比 / 文案三层叠在一起，间距收到 10rpx 让它们读起来是**一组**而不是三件东西 */
  gap: 10rpx;
  /* 文案可能换行，留出左右边距别顶到盒子边上 */
  padding: 0 24rpx;
  text-align: center;
  /* 唯一的动效：整块轻微呼吸，表示还在跑。渐变本身不动，免得喧宾夺主 */
  animation: gen-breathe 1.8s ease-in-out infinite;
}

.gen-canvas__icon {
  width: 52rpx;
  height: 52rpx;
}

.gen-canvas__value {
  color: #ffffff;
  font-size: 32rpx;
  font-weight: 600;
  letter-spacing: 1rpx;
  /* 渐变亮处白字会发飘，垫一层很淡的暖影 */
  text-shadow: 0 2rpx 8rpx rgba(120, 92, 74, 0.22);
}

@keyframes gen-breathe {
  50% {
    opacity: 0.68;
  }
}

@keyframes blink {
  50% {
    opacity: 0;
  }
}

/* 常驻在每张图下方（2026-08-10 需求 7），不再是长按唤起的浮层，所以去掉了投影
   ——常驻元素带 28rpx 的投影会把每张图下面都糊出一道灰边。 */
.image-actions {
  display: flex;
  align-items: center;
  align-self: flex-start;
  height: 68rpx;
  margin-top: 12rpx;
  padding: 0 14rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.92);
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.86);
}

/* .bubble-imgs 是 gap:14rpx 的纵向 flex：操作条再叠 margin-top 就会离**自己那张图** 26rpx、
   离下一张图只有 14rpx，多图气泡里分不清这条属于谁。改成上边距归零、下边距补 10rpx，
   一眼就是「贴着上面那张图」。*/
.bubble-imgs .image-actions {
  margin-top: 0;
  margin-bottom: 10rpx;
}

/* 用户自己发的图气泡只有 430rpx 宽（内容区 410rpx），三颗按钮要收一收才不撑破气泡。
   .bubble--image 是块级容器，这条自然铺满内容区，靠 space-between 把三颗按钮摊开。
   它长在白色气泡**内部**，所以不再画自己的白底白框（白压白看不出边界），
   改成一条顶部细分隔线，读起来就是气泡的一部分。 */
.image-actions--user {
  justify-content: space-between;
  height: 64rpx;
  margin-top: 8rpx;
  padding: 0 6rpx;
  border: 0;
  border-top: 2rpx solid rgba(60, 50, 40, 0.08);
  border-radius: 0;
  background: transparent;
}

.image-actions--user .image-action {
  padding: 0 8rpx;
  font-size: 23rpx;
}

.image-action {
  display: flex;
  align-items: center;
  gap: 10rpx;
  height: 100%;
  padding: 0 14rpx;
  color: #2f2f2f;
  font-size: 25rpx;
}

.image-action__icon {
  width: 25rpx;
  height: 25rpx;
}

.image-action--danger {
  color: #ef4545;
}

.image-actions__divider {
  width: 2rpx;
  height: 34rpx;
  background: #dedbd7;
}

.bottom-anchor {
  height: 26rpx;
}

/* 回复期间在底部留一条空带，免得「停止生成」压住正在打出来的字。
   ⚠️ 只对**没有占位盒**的那一段有效——出图那一段被下面 .msg-row--gen 那条覆盖掉。*/
.bottom-anchor--sending {
  height: 150rpx;
}

/* —— 生图中：占位盒底部与输入框对齐（2026-08-07 需求 1）——
   原来占位盒下面依次垫着 气泡内边距 20rpx + 行间距 20rpx + 贴底空带 150rpx，
   盒子被顶在半空、下方空一大截。这三层在占位盒在场时全部收掉，贴底后盒子底边就正好
   落在输入框上沿（只剩 .input-area 自己的 8rpx 外边距）；「停止生成」改为悬浮在盒子偏下方，
   不再需要那条空带给它腾位置。
   ⚠️ 三条规则必须同时在：少收一层就差那一层的高度，看着就是「差一点点没对齐」。
   .msg-row--gen 只挂在 item.streaming 的那条消息上（100% 时 streaming 落下，样式随之复位）。*/
.msg-row--gen {
  margin-bottom: 0;
}

.bubble--ai.bubble--gen {
  padding-bottom: 0;
  /* 盒子（22rpx）与气泡（28rpx）圆角对齐，贴到底才不会在两个下角露出渐变的尖角 */
  border-bottom-left-radius: 22rpx;
  border-bottom-right-radius: 22rpx;
}

/* 选择器权重 0,2,0 > .bottom-anchor--sending 的 0,1,0，出图那一段稳定压住 150rpx */
.msg-row--gen + .bottom-anchor {
  height: 0;
}

/* ==================== 失败卡片 ==================== */
.bubble--ai.bubble--failed {
  position: relative;
  width: 520rpx;
  max-width: 520rpx;
  padding: 10rpx;
  overflow: visible;
}

.failure-card__delete {
  position: absolute;
  top: -54rpx;
  right: -18rpx;
  z-index: 2;
  display: flex;
  align-items: center;
  gap: 9rpx;
  padding: 15rpx 24rpx;
  border-radius: 22rpx;
  color: #ef4545;
  background: rgba(255, 255, 255, 0.94);
  box-shadow: 0 10rpx 24rpx rgba(74, 62, 46, 0.1);
  font-size: 24rpx;
}

.failure-card__delete image {
  width: 28rpx;
  height: 28rpx;
}

.failure-card__visual {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 400rpx;
  padding: 38rpx;
  border-radius: 22rpx;
  background:
    radial-gradient(circle at 50% 28%, rgba(255, 255, 255, 0.78), transparent 230rpx),
    linear-gradient(180deg, #fff0e7 0%, #f5eee8 100%);
}

.failure-card__title {
  color: #2b2b2b;
  font-size: 34rpx;
  font-weight: 600;
}

.failure-card__desc {
  margin-top: 16rpx;
  color: #85817d;
  font-size: 25rpx;
  text-align: center;
}

.failure-card__retry {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14rpx;
  min-width: 260rpx;
  height: 72rpx;
  margin-top: 32rpx;
  border-radius: 999rpx;
  color: #ffffff;
  background: linear-gradient(90deg, #ed8a69, #ed744f);
  box-shadow: 0 14rpx 26rpx rgba(226, 111, 75, 0.22);
  font-size: 27rpx;
  font-weight: 500;
}

.retry-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36rpx;
  height: 36rpx;
  font-size: 36rpx;
  line-height: 1;
}

/* ==================== 输入区 ==================== */
/* 停止生成：锚在 .input-area（position: relative）上，bottom:100% 就是输入框上沿，
   再往上抬 GAP 一点点。这样输入框长高（待发图缩略图那一条）也不用改这里的数字
   —— 原先写死 fixed bottom:450rpx，是照「输入框大概多高」猜的，一变就飘。
   生图时占位盒底部已经贴到输入框（见 .msg-row--gen），所以这颗药丸正好悬浮在盒子偏下的位置。*/
.stop-pill {
  position: absolute;
  left: 50%;
  bottom: calc(100% + 16rpx);
  z-index: 14;
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 13rpx 28rpx;
  border-radius: 999rpx;
  color: #555555;
  background: rgba(255, 255, 255, 0.95);
  box-shadow: 0 10rpx 28rpx rgba(74, 62, 46, 0.12);
  font-size: 25rpx;
  transform: translateX(-50%);
}

.stop-square {
  width: 18rpx;
  height: 18rpx;
  border-radius: 4rpx;
  background: #ef641e;
}

.banned-bar {
  position: relative;
  z-index: 2;
  flex: 0 0 auto;
  margin: 0 32rpx 12rpx;
  padding: 11rpx 20rpx;
  border-radius: 16rpx;
  color: #d64541;
  background: #fdecec;
  font-size: 23rpx;
  text-align: center;
}

.input-area {
  position: relative;
  z-index: 12;
  flex: 0 0 auto;
  margin: 8rpx 32rpx 18rpx;
  padding: 18rpx 20rpx 20rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.94);
  border-radius: 42rpx;
  background: rgba(255, 255, 255, 0.84);
  box-shadow: 0 20rpx 44rpx rgba(91, 72, 45, 0.16);
}

.pending-strip {
  width: 100%;
  margin-bottom: 14rpx;
  white-space: nowrap;
}

.pending-thumb {
  position: relative;
  display: inline-block;
  width: 100rpx;
  height: 100rpx;
  margin-right: 14rpx;
  overflow: hidden;
  border-radius: 16rpx;
  background: #eee9e2;
  vertical-align: top;
}

.pending-thumb__img {
  width: 100%;
  height: 100%;
}

.pending-thumb__mask {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(15, 20, 30, 0.42);
}

.pending-thumb__spin {
  width: 30rpx;
  height: 30rpx;
  border: 4rpx solid rgba(255, 255, 255, 0.35);
  border-top-color: #ffffff;
  border-radius: 50%;
  animation: pending-spin 0.8s linear infinite;
}

@keyframes pending-spin {
  to {
    transform: rotate(360deg);
  }
}

.pending-thumb__del {
  position: absolute;
  top: 0;
  right: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36rpx;
  height: 36rpx;
  color: #ffffff;
  background: rgba(15, 20, 30, 0.55);
  border-bottom-left-radius: 14rpx;
  font-size: 26rpx;
  line-height: 1;
}

.quick-tools {
  display: grid;
  grid-template-columns: 1fr 1fr 1.35fr 1.75fr;
  align-items: center;
  gap: 8rpx;
}

.quick-tool,
.orientation-button,
.generate-button {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 9rpx;
  min-width: 0;
  height: 58rpx;
  border: 2rpx solid rgba(125, 125, 125, 0.18);
  border-radius: 999rpx;
  color: #6f6f6f;
  background: rgba(255, 255, 255, 0.42);
  font-size: 25rpx;
  white-space: nowrap;
}

.quick-tool__icon {
  width: 27rpx;
  height: 27rpx;
}

.orientation-button {
  border-color: rgba(125, 125, 125, 0.18);
}

.orientation-button--active {
  color: #f26724;
  border-color: #f5a77e;
  background: #fff4ed;
}

.orientation-button__icon {
  width: 27rpx;
  height: 27rpx;
  opacity: 0.75;
}

/* 原来是 ›/× 两个字符：字形本身在行盒里偏上、两者字宽还不一样，
   一开合就把按钮里的图标+文字整体顶偏。换成固定 34rpx 的方图，
   两态同盒同大小、图源自身居中 —— 切换时零位移。*/
.orientation-button__tail {
  flex: 0 0 auto;
  width: 22rpx;
  height: 22rpx;
}

.generate-button {
  color: #ffffff;
  border-color: transparent;
  background: linear-gradient(90deg, #ff5f24, #ff884a);
  box-shadow: 0 10rpx 22rpx rgba(255, 104, 39, 0.22);
  font-weight: 500;
}

.generate-button--active {
  color: #f26724;
  border: 2rpx solid #f26724;
  background: rgba(255, 255, 255, 0.78);
  box-shadow: none;
}

/* 外层固定 36rpx，魔杖/关闭切换不改宽度；× 的图源四周留白多(实际笔画只占 64%)，
   所以关闭态把图放大到 36rpx，才和魔杖看着一样大。*/
.generate-button__icon {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36rpx;
  height: 36rpx;
}

.generate-button__icon-img {
  width: 29rpx;
  height: 29rpx;
}

.generate-button__icon-img--close {
  width: 36rpx;
  height: 36rpx;
}

.input-line {
  display: flex;
  align-items: center;
  gap: 14rpx;
  min-height: 82rpx;
  margin-top: 16rpx;
  /* 右侧是 70rpx 的圆按钮，上下各留 6rpx，右边也给 6rpx 才是四周等距 */
  padding: 0 6rpx 0 24rpx;
  border: 2rpx solid rgba(108, 108, 108, 0.14);
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.34);
}

.input-wrap,
.voice-hold {
  flex: 1;
  min-width: 0;
}

.chat-input {
  width: 100%;
  height: 78rpx;
  color: #3a3a3a;
  background: transparent;
  font-size: 30rpx;
}

.voice-hold {
  height: 68rpx;
  border-radius: 999rpx;
  color: #555555;
  background: #f2eee9;
  font-size: 28rpx;
  line-height: 68rpx;
  text-align: center;
}

.voice-hold--recording {
  color: #ffffff;
  background: linear-gradient(90deg, #ff6c2a, #ff884a);
}

.send-image-button,
.voice-switch {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 70rpx;
  height: 70rpx;
}

/* ai-send-active.png 是 141×141 的画布，实心橙圆只占中间 64px = 45.4%，其余全是外发光/投影：
   原来图按 70rpx 画，圆实际只有 31.8rpx，比左邻的麦克风(48rpx)还小一圈 —— 需求「放大一点」。
   图放到 114rpx，圆就是 114×45.4% ≈ 52rpx；外发光(占画布 70.2%)展开 80rpx，只比 70rpx 的
   点击盒探出 5rpx，落在 .input-line 右侧 6rpx 内边距里，不会溢出输入条的圆角边框。
   圆在画布里的中心是 y=63.5 而不是 70（投影往下糊），直接居中会看着偏上 ——
   再下推 (70−63.5)/141 = 4.61% 自身高度，让「圆」而不是「圆+阴影」上下居中（需求原话）。
   点击命中仍只认 70rpx 的盒子，溢出的发光 pointer-events:none，免得压住输入框右端。*/
.send-image-button__icon {
  flex: 0 0 auto;
  width: 114rpx;
  height: 114rpx;
  transform: translateY(4.61%);
  pointer-events: none;
}

.send-image-button--disabled {
  opacity: 0.42;
}

.voice-placeholder__image {
  width: 48rpx;
  height: 48rpx;
}

.tool-popover {
  position: absolute;
  bottom: calc(100% + 18rpx);
  z-index: 18;
  border: 2rpx solid rgba(255, 255, 255, 0.92);
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.97);
  box-shadow: 0 18rpx 42rpx rgba(77, 62, 43, 0.18);
}

/* 居中对齐到方向按钮：quick-tools 是 1fr 1fr 1.35fr 1.75fr + 8rpx 间距，
   算下来第 3 格中心在距 .input-area padding box 左边 360rpx 处，
   left 恒等于「360 − 弹层宽/2」。2026-08-10 加了标题后弹层由 250rpx 放宽到 304rpx，
   left 随之从 235rpx 改到 208rpx —— 改宽度必须同步改 left，否则整体偏右。*/
.orientation-popover {
  left: 208rpx;
  width: 304rpx;
  padding: 12rpx;
}

/* 弹层标题（2026-08-10 需求 3）：「设置文生图比例」7 个字按 26rpx 约 182rpx，
   原来 250rpx 的弹层减去 32rpx 内边距只剩 226rpx，勉强够但顶到边，所以一并放宽到 304rpx。*/
.orientation-popover__title {
  padding: 8rpx 16rpx 12rpx;
  color: #2c2c2c;
  font-size: 26rpx;
  font-weight: 600;
}

.orientation-option {
  display: grid;
  grid-template-columns: 28rpx 1fr 24rpx;
  align-items: center;
  gap: 14rpx;
  height: 72rpx;
  padding: 0 16rpx;
  border-radius: 22rpx;
  color: #303030;
  font-size: 28rpx;
}

.orientation-option--active {
  color: #f26724;
  background: #fff0e7;
}

.orientation-option__image {
  width: 28rpx;
  height: 28rpx;
}

.orientation-check {
  width: 24rpx;
  height: 24rpx;
}

/* 右边缘对齐「一键生图」按钮：第 4 格右边缘距 padding box 右侧正好 20rpx（原写 0，多探出 20rpx）*/
.style-popover {
  right: 20rpx;
  width: 310rpx;
  padding: 22rpx 24rpx;
}

.style-popover__header {
  display: flex;
  align-items: center;
  gap: 14rpx;
  margin-bottom: 14rpx;
}

.style-popover__mark {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 54rpx;
  height: 54rpx;
  border-radius: 50%;
}

.style-popover__mark image {
  width: 54rpx;
  height: 54rpx;
}

.style-popover__title {
  color: #2c2c2c;
  font-size: 29rpx;
  font-weight: 600;
}

.style-popover__desc {
  margin-top: 2rpx;
  color: #9b9691;
  font-size: 20rpx;
}

.style-option {
  display: grid;
  grid-template-columns: 14rpx 1fr 28rpx;
  align-items: center;
  gap: 14rpx;
  height: 64rpx;
  color: #303030;
  font-size: 27rpx;
}

.style-option__dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: #ff7840;
}

.style-option__arrow {
  width: 28rpx;
  height: 28rpx;
}

/* ==================== 录音动效 ==================== */
.voice-cover {
  position: absolute;
  inset: 0;
  z-index: 30;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 13rpx;
  padding: 20rpx 32rpx;
  border-radius: 42rpx;
  background: rgba(255, 255, 255, 0.98);
  pointer-events: none;
}

.voice-cover--cancel {
  background: #fdecec;
}

.voice-wave {
  display: flex;
  align-items: center;
  gap: 9rpx;
  height: 52rpx;
}

.voice-wave__bar {
  width: 8rpx;
  height: 20rpx;
  border-radius: 999rpx;
  background: #ef641e;
  animation: voiceWave 0.9s ease-in-out infinite;
}

.voice-cover--cancel .voice-wave__bar {
  background: #d64541;
}

.voice-wave__bar:nth-child(2) { animation-delay: 0.12s; }
.voice-wave__bar:nth-child(3) { animation-delay: 0.24s; }
.voice-wave__bar:nth-child(4) { animation-delay: 0.36s; }
.voice-wave__bar:nth-child(5) { animation-delay: 0.48s; }
.voice-wave__bar:nth-child(6) { animation-delay: 0.6s; }
.voice-wave__bar:nth-child(7) { animation-delay: 0.72s; }

@keyframes voiceWave {
  0%,
  100% {
    height: 20rpx;
  }
  50% {
    height: 50rpx;
  }
}

.voice-tip {
  color: #8b8681;
  font-size: 24rpx;
  text-align: center;
}

.voice-cover--cancel .voice-tip {
  color: #d64541;
}

.voice-tip--warn {
  color: #eb8f1b;
  font-size: 21rpx;
}

/* ==================== 投屏设备选择 ==================== */
.sheet-mask {
  position: fixed;
  inset: 0;
  z-index: 40;
  display: flex;
  align-items: flex-end;
  background: rgba(30, 29, 27, 0.42);
}

.device-sheet {
  width: 100%;
  max-height: 78vh;
  padding: 32rpx 48rpx calc(26rpx + var(--safe-bottom));
  border-top-left-radius: 42rpx;
  border-top-right-radius: 42rpx;
  background: rgba(250, 248, 244, 0.98);
  box-shadow: 0 -14rpx 40rpx rgba(50, 43, 34, 0.12);
  animation: device-sheet-in 0.28s ease-out;
}

@keyframes device-sheet-in {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.device-sheet__title {
  color: #252525;
  font-size: 38rpx;
  font-weight: 600;
  text-align: center;
}

.device-sheet__desc {
  margin-top: 10rpx;
  color: #8d8984;
  font-size: 25rpx;
  text-align: center;
}

.device-sheet__divider {
  height: 2rpx;
  margin: 25rpx 0 20rpx;
  background: #dfdbd5;
}

.sheet-loading {
  min-height: 260rpx;
  padding: 44rpx 0;
}

.device-list {
  max-height: 560rpx;
}

.device-item {
  display: grid;
  grid-template-columns: 88rpx 1fr 48rpx;
  align-items: center;
  gap: 22rpx;
  min-height: 120rpx;
  margin-bottom: 16rpx;
  padding: 16rpx 26rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.92);
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.88);
}

.device-item--selected {
  border-color: #df7654;
  background: #fff8f4;
}

.device-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 76rpx;
  height: 76rpx;
  border-radius: 22rpx;
  color: #ff6e28;
  background: #fff0e9;
  font-size: 23rpx;
}

.device-placeholder--1 {
  color: #2dad6f;
  background: #eaf8f0;
}

.device-placeholder--2 {
  color: #5794ef;
  background: #eaf2ff;
}

.device-name {
  overflow: hidden;
  color: #2c2c2c;
  font-size: 30rpx;
  font-weight: 500;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.device-radio {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 42rpx;
  height: 42rpx;
  border: 3rpx solid #c5c2be;
  border-radius: 50%;
}

.device-radio--selected {
  border-color: #e87c5a;
  background: #e87c5a;
}

.device-radio__check {
  width: 18rpx;
  height: 10rpx;
  border-bottom: 5rpx solid #ffffff;
  border-left: 5rpx solid #ffffff;
  transform: rotate(-45deg);
}

.device-sheet__cancel {
  height: 84rpx;
  margin-top: 24rpx;
  border-radius: 999rpx;
  color: #272727;
  background: #eeeae5;
  font-size: 31rpx;
  font-weight: 500;
  line-height: 84rpx;
  text-align: center;
}

/* ==================== 删除消息确认框 ====================
   版式与 subpackages/album/list 的删除弹窗一致（图标 + 标题/说明 + 取消/确认），
   全站弹框保持同一套观感；原来这里用的是 wx.showActionSheet 原生面板。*/
.confirm-mask {
  position: fixed;
  inset: 0;
  z-index: 60;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 48rpx;
  background: rgba(0, 0, 0, 0.4);
}

.confirm-dialog {
  display: grid;
  grid-template-columns: 80rpx 1fr;
  column-gap: 28rpx;
  width: 100%;
  padding: 60rpx 48rpx 40rpx;
  border-radius: 28rpx;
  background: #ffffff;
  animation: consent-dialog-in 0.24s ease-out;
}

.confirm-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
  border-radius: 18rpx;
  background: rgba(255, 106, 32, 0.08);
}

.confirm-icon__image {
  width: 34rpx;
  height: 34rpx;
}

.confirm-copy {
  min-width: 0;
}

.confirm-title {
  color: #25282d;
  font-size: 38rpx;
  font-weight: 700;
  line-height: 1.15;
}

.confirm-desc {
  margin-top: 12rpx;
  color: #6f7782;
  font-size: 24rpx;
  line-height: 1.45;
}

.confirm-actions {
  grid-column: 1 / 3;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
  margin-top: 42rpx;
  padding-left: 118rpx;
}

.confirm-cancel,
.confirm-primary {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 72rpx;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 600;
}

.confirm-cancel {
  color: #32363c;
  background: #eeeeee;
}

.confirm-primary {
  color: #ffffff;
  background: linear-gradient(180deg, #ff9140 0%, #ff6a20 100%);
}

/* ==================== AI 服务协议确认框 ==================== */
.consent-mask {
  position: fixed;
  inset: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 48rpx;
  background: rgba(27, 31, 38, 0.5);
}

.consent-dialog {
  width: 100%;
  max-width: 654rpx;
  padding: 44rpx 40rpx 36rpx;
  border-radius: 32rpx;
  background: #ffffff;
  box-shadow: 0 24rpx 72rpx rgba(20, 28, 42, 0.2);
  animation: consent-dialog-in 0.2s ease-out;
}

.consent-dialog__title {
  color: #25282d;
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1.25;
  text-align: center;
}

.consent-dialog__content {
  margin-top: 28rpx;
  color: #636a74;
  font-size: 26rpx;
  line-height: 1.65;
  text-align: left;
}

.consent-dialog__paragraph + .consent-dialog__paragraph {
  margin-top: 20rpx;
}

.consent-dialog__actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
  margin-top: 40rpx;
}

.consent-dialog__button {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 76rpx;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 500;
}

.consent-dialog__button--cancel {
  color: #32363c;
  background: #eeeeee;
}

.consent-dialog__button--confirm {
  color: #ffffff;
  background: linear-gradient(180deg, #ff9140, #ff6a20);
}

@keyframes consent-dialog-in {
  from {
    opacity: 0;
    transform: scale(0.96);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
