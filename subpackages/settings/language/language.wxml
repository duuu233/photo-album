<view class="page safe-bottom">
  <view class="language-card card" wx:if="{{languageInfo}}">
    <view class="language-title">{{languageInfo.label}}</view>
    <view class="caption">当前手机系统语言：{{languageInfo.raw}}</view>
  </view>

  <view class="section card">
    <view class="info-row">
      小程序启动时会读取微信系统信息中的语言设置，并把标准化后的语言值提交给登录接口。这里不提供手动切换入口。
    </view>
  </view>
</view>
