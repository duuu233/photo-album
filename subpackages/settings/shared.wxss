/* 全站统一背景：和 AI 页 / 各页 .mock-bg 用的是同一张 OSS 图（750×1624，正好 2× 屏宽）。
   协议类、更新页没有 .mock-bg 图层，正文又是不定位的普通块（fixed+z-index:0 的图层会盖住它们），
   所以这里走 CSS background —— 背景永远画在内容之下，不用给每块正文补 z-index。
   已经有 .mock-bg 的页面（设置/语种/指南/个人信息）叠的是同一张图，视觉无差别。
   底色 #f8f5ef 是图没加载出来前的兜底，取自 AI 页根底色。*/
.settings-shell {
  position: relative;
  min-height: 100vh;
  overflow: hidden;
  color: #2a2d32;
  background: #eff3f8 url(https://oss.boltfox.cn/prodFile/202608211340094498724.jpg) center / cover no-repeat;
}

/* 折叠屏适配（2026-08-05）：设置类页面套 .fold-viewport + .fold-scroll 后，
   滚动内容块统一再加这个类（方案说明见 styles/fold-adapt.wxss）。

   「退出登录 / 保存设置 / 立即更新 + 用户注销 / 确认」这一摞按钮是
   `position: absolute; bottom: …`，定位父级仍是**根容器**（它们留在滚动区外，位置一像素没动）。
   padding-bottom 让出的就是这摞按钮占的高度：它们脱离文档流，不预留的话变长的内容会从底下穿过去。
   352rpx = 最高的一枚（bottom 216rpx + 高 112rpx）再留 24rpx 余量；`--safe-bottom` 是
   Home Indicator 的避让，与按钮各自的 bottom 口径保持一致。
   内容装得下时这块 padding 落在屏幕外的滚动区末端，一个像素都看不出来。 */
.settings-scroll-body {
  padding-bottom: calc(352rpx + var(--safe-bottom, 0px));
}

.nav-bar {
  position: relative;
  z-index: 2;
  height: calc(var(--status-bar) + 88rpx);
}

.back-btn {
  position: absolute;
  left: 44rpx;
  bottom: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
  color: #565d67;
  background: rgba(255, 255, 255, 0.42);
  border: 2rpx solid rgba(255, 255, 255, 0.88);
  border-radius: 50%;
  font-size: 38rpx;
  line-height: 1;
}

.nav-title {
  position: absolute;
  right: 120rpx;
  bottom: 28rpx;
  left: 120rpx;
  color: #111111;
  font-size: 32rpx;
  font-weight: 700;
  line-height: 1;
  text-align: center;
}

.glass-card {
  position: relative;
  z-index: 1;
  margin: 68rpx 48rpx 0;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.72);
  border: 2rpx solid rgba(255, 255, 255, 0.88);
  border-radius: 22rpx;
  box-shadow: 0 24rpx 52rpx rgba(128, 151, 184, 0.12);
}

  /* 底图改用 primary-btn-bg.png（2026-08-21 全站主按钮统一）：图是 <image class="cta-button-bg"> 子节点，
     WXSS 引不了本地图，只能这么挂。原来的橙色渐变与外发光 box-shadow 一并去掉——发光已画在图里，
     两层叠着会糊出一圈脏边。position:relative 是底图定位的父级，不可省。 */
.primary-btn {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 112rpx;
  color: #ffffff;
  border-radius: 999rpx;
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1;
}

.disabled-btn {
  color: rgba(255, 255, 255, 0.82);
  background: #e1e7f0;
  box-shadow: none;
}

/* 禁用态把底图藏掉，露出上面那层灰底（否则橙色底图盖着，按钮看着还是可点的） */
.disabled-btn .cta-button-bg {
  display: none;
}

.row {
  position: relative;
  display: flex;
  align-items: center;
  min-height: 124rpx;
  padding: 0 36rpx;
  text-align: left;
}

.row + .row::before {
  position: absolute;
  top: 0;
  right: 36rpx;
  left: 36rpx;
  height: 1rpx;
  background: rgba(207, 214, 224, 0.72);
  content: '';
}

.row-label {
  color: #2a2d32;
  font-size: 28rpx;
  font-weight: 600;
}

.row-value,
.placeholder {
  color: #808690;
  font-size: 28rpx;
}

.chevron {
  width: 28rpx;
  height: 28rpx;
  flex-shrink: 0;
}

.modal-mask {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 20;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(42, 43, 43, 0.5);
}

.dialog {
  width: 654rpx;
  padding: 70rpx 92rpx 32rpx;
  background: #ffffff;
  border-radius: 34rpx;
  text-align: center;
  animation: popDialogIn 0.24s ease-out;
}

.dialog-title {
  color: #2a2d32;
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1;
}

.dialog-desc {
  margin-top: 22rpx;
  color: #636a74;
  font-size: 24rpx;
  line-height: 1.45;
}

.dialog-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 40rpx;
  margin-top: 42rpx;
}

.dialog-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 72rpx;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 700;
  line-height: 1;
}

.cancel-btn {
  color: #2a2d32;
  background: #eeeeee;
}

.confirm-btn {
  color: #ffffff;
  background: linear-gradient(90deg, #ff8338, #ff621f);
}
