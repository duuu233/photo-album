# 星币套餐卡赠送角标位置统一与卡片收窄

> 状态：Historical
> 日期：2026-09-08
> 环境：SSH 开发机
> 分支与起始版本：main / 0b1a7b3
> 范围：小程序星币管理页面的套餐卡

## 变更

### 1. 赠送角标：选中与未选中位置一致

原来两种状态画在两个位置：未选中是卡片内右对齐的橙色文字，选中才变成实心橙标签浮到卡片右上角，
点选时角标会跳一下。现在 `.package-gift` 基础样式直接带上 `position: absolute; top/right: -3rpx`
与圆角、内边距，两种状态几何完全相同，`.package-card--active .package-gift` 只覆盖
`color` 与 `background`：未选中是透明底橙字，选中是橙底白字。

⚠️ `.package-gift-slot` 那 32rpx 占位**更不能删**：角标现在恒为 `absolute`、任何状态下都不占高，
而卡片是 `justify-content: center`，占位一旦没了，有赠送和无赠送的卡片内容会差半格。

### 2. 卡片收窄，右侧留白变少

**当日收窄两次**：`.package-card` 宽度 300rpx → 276rpx → **248rpx**（`flex-basis` 同步），
左右内边距 22rpx → 20rpx → **16rpx**。300rpx 是上一轮为「六位整数 + 两位小数」留的余量，
产品复看两次都嫌右侧空。主价格字号自始至终没动（仍 40rpx）。

⚠️ **248rpx 是不动字号能到的下限**：内宽 248 − 16×2 = 216rpx，而 `￥999999.99` 在 40rpx
字号下约 200rpx，余量只剩约 16rpx。再要更窄必须先把 `.package-price-num` 降到 36rpx
（六位金额的需求还在，不能靠折行或省略号糊过去）。

## 同步与验证

修改前在 main 执行 `git pull --ff-only`。本机无 CodeGraph 索引，未执行索引同步。

`node --test tests/token-pay.test.js` 通过。⚠️ 本轮是纯样式改动，现有用例覆盖不到几何，
**真机与视觉验收未做**，需要核对：有赠送 / 无赠送 × 选中 / 未选中四种卡片的角标位置与内容对齐、
六位金额（999999.99）在 276rpx 下是否仍完整不折行、横向滚动的手感。

## 后续与回滚

APP 端同一套改法，见 `flutter/docs/history/2026-09/2026-09-08-star-package-card-badge-width.md`。
需要回滚时 revert 本次提交，仅样式与模板注释受影响。
