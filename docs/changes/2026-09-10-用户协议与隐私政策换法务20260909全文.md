# 用户协议与隐私政策换成法务 20260909 全文，AI 协议违规阶梯改口径（2026-09-10）

> 状态：current
> 最后核对：2026-09-10
> 适用范围：微信小程序（`subpackages/settings/{privacy,agreement,ai-agreement}`）+ Flutter App
> （`lib/src/features/settings/presentation/{privacy_policy_page,user_agreement_page,ai_service_agreement_page}.dart`）
> 权威来源：Project Hub `docs/相册协议/` 下的四份法务 docx（文件名 20260909，正文页首写的更新日期是
> **2026 年 8 月 28 日**）——中文两份、名字带 `(EN)` 的英文两份
> 相关：[2026-08-13 隐私政策换法务 v3 全文](2026-08-13-隐私政策换法务v3全文.md)（上一版，做法沿用）

## 这轮改了什么

产品口径：**小程序端固定简中，App 端固定英文**，两端各取法务同一批文档里对应语言的那一份。

| 端 | 用户协议 | 隐私政策 | AI 服务协议 |
| --- | --- | --- | --- |
| 小程序 | 旧短文 → 法务简中全文（15 章） | v3.0 → 法务简中全文（14 章） | 违规阶梯表改口径 |
| App | 旧短文（中英日三份译文） → 法务英文全文（15 章），**固定英文** | v3.0 英文 → 法务英文全文（14 章），仍固定英文 | 违规阶梯表改口径（中英日三份都改） |

### 小程序

| 文件 | 改动 |
| --- | --- |
| `subpackages/settings/privacy/privacy-content.js` | 整份替换为新版简中正文（14 章 / 56 条列表 / 4 张表） |
| `subpackages/settings/agreement/agreement-content.js` | **新增**：新版简中用户协议正文（15 章 / 60 条列表 / 1 张表） |
| `subpackages/settings/agreement/agreement.wxml` | 由写死的三节旧短文改为一套通用渲染，与 `privacy.wxml` 同模板 |
| `subpackages/settings/agreement/agreement.js` | 从 `agreement-content` 取数进 `data`，补 `wx:key` 用的稳定下标 key |
| `subpackages/settings/ai-agreement/ai-agreement.wxml` | 第 4 节违规处理表：原来「第1/2/3次」三行各封 24 小时，改成一行「第1-3次｜AI功能提示违禁」 |
| `tests/privacy-policy-content.test.js` | 断言改到新版日期，并新增「页面不许写死旧版日期」 |
| `tests/user-agreement-content.test.js` | **新增**：15 章齐全、违规表列数对齐、关键章节没丢、页面不残留旧短文 |

`agreement.wxss` 仍是 `@import "../privacy/privacy.wxss"`，`.article-meta` / `.article-sub-title` /
`.article-table*` 直接复用，没有新增样式。

### App

| 文件 | 改动 |
| --- | --- |
| `privacy_policy_page.dart` | 正文换成新版英文（仍是编译期常量 `privacyPolicySections`，**不走** `AppL10n.pick`），页首日期改 August 28, 2026 |
| `user_agreement_page.dart` | **整页重写**：由 `l10n.pick` 的中英日三份短文改成编译期常量 `userAgreementSections`（15 章），固定英文 |
| `ai_service_agreement_page.dart` | 第 4 节违规阶梯：中、英、日**三份都改**成「第1-3次＝提示违禁」 |
| `test/privacy_policy_content_test.dart` | 注释与用例名跟到新版 |
| `test/user_agreement_content_test.dart` | **新增**：15 章齐全、违规表列数对齐、正文无中文（＝没被 l10n 化）、常量身份不变 |

## 关键决策

- **正文一律逐块转换，不手写**。四份 docx 用一次性脚本（解析 `word/document.xml`，按样式名
  `heading 2/3` 与 `numPr` 分区块）转成两端的数据结构，脚本未入仓——与 2026-08-13 那轮同一套做法。
  Dart 侧的长字符串按空格切成相邻字面量，**转换后逐块比对过拼接结果与原文逐字相同**。
- **目录（TOC）没有转换进来**。两端都没有锚点跳转能力，照抄只会多出一屏点不动的条目；
  两份文档的 14 / 15 条目录都丢弃，正文一字未改。
- **App 的用户协议改成固定英文**，与隐私政策同一口径：法务只出具中英两版，日文没有经审阅的
  文本；机翻一份法律文本摆进设置页比只给英文更糟。页面标题仍随语种（那只是入口名字，不是条款）。
- **AI 服务协议不是法务这批文档的一部分**，违规阶梯是产品本轮单独给的口径，因此照旧改在页面里，
  没有并进 `*-content.js` / `*Sections` 那套数据。App 侧连日文一起改：只改英文会让切到日文的用户
  看到已经作废的阶梯。

## ⚠️ 需要法务 / 产品回话的四件事（端上一律照抄，没有自行修改）

1. **生效日期是占位符**。简中隐私政策写「生效日期：待填写」、简中用户协议写
   「生效日期：[待填写：生效日期]」，英文两份同理（`To be completed`）。端上原样展示，
   **发版前必须补上真实日期**。
2. **隐私政策的版本号写作 `VI.0`**（用户协议是 `V1.0`），疑为 `V1.0` 的笔误。
3. **英文版有若干断词/断句缺口**，是原文里粗体 run 的边界造成的，Word 里同样是这个样子：
   - 句号后少空格 6 处（英文用户协议）：`bold.By`、`Agreement.If`、`app.If`、`prevail.Functions`、
     `others.You`、`Account".After`；
   - 标点前多一个空格 10 处（英文隐私政策 9 处 + 用户协议 1 处）：如
     `Permission: Bluetooth . Used`、`payment passwords ; we only`、`(App Store) ; in the WeChat`。
   两端都不渲染粗体，这些缺口在页面上会更显眼。
4. **英文隐私政策第 13 节的公司名与页首、页尾不一致**：正文开头与结尾是
   `BoltStar (Shenzhen) New Energy Technology Co., Ltd.`，第 13 节写的是
   `Qihe Ming (Shenzhen) New Energy Technology Co., Ltd.`。

## ⚠️ 用户协议第九章与 AI 服务协议现在对不上

法务这份用户协议第九章的违规处理表仍是「第 1 次 / 第 2 次 / 第 3 次 各封 AI 功能 24 小时」，
并且原文写明**「与应用内《BoltStar AI 服务协议》一致」**；而本轮按产品口径把 AI 服务协议改成了
「第 1-3 次只提示违禁、第 4 次起才封 24 小时」。**两份文档现在互相矛盾。**

端上没有替法务改用户协议的正文——法律文本只能由法务出新版。请产品与法务确认以哪一份为准：
若以新口径为准，需要法务重出用户协议第九章那张表，端上再重新转换一次。

## 回归验证

- [x] 小程序：`node --test "tests/*.test.js"` **57 / 58 通过**。唯一失败的是
      `tests/token-page-layout.test.js`（断言 `subpackages/token/index/index.wxss` 里的
      `.package-card--active .package-gift` 规则），**与本轮无关**：该规则在 2026-09-08 的
      `487869b`「赠送角标固定为橙底白字」里就被去掉了，本轮一个字都没动 `subpackages/token/`。
- [ ] App：`flutter analyze` / `flutter test` **未执行**（本机无 Flutter SDK）。只做了静态自检：
      两页与两个用例文件过了括号配平检查，并逐块校验过 Dart 字符串拼回原文一致、正文里没有中文。
- [ ] 真机：两端四页（用户协议 / 隐私政策 / AI 服务协议）从头滚到尾，看表格在窄屏上不裁字不串行；
      App 侧切到简中 / 繁中 / 日文后**用户协议与隐私政策正文仍是英文**、AI 服务协议仍随语种。
- [ ] 法务 / 产品：上面那四件事 + 用户协议与 AI 协议的矛盾。
