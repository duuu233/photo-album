# 2026-09-18 圆形（圆屏预览）逻辑整体回滚 —— 等真用上再加回来

> 状态：**已回滚，双端同步**
> 起始版本：小程序 `04dbc20`
> 取代：`2026-09-17-圆屏产品预览裁圆导出仍方.md`（那轮加的代码本轮全部撤掉，文档留档备查）

## 为什么撤

产品侧现在**还用不上圆屏**（没有在售的圆形产品走这条路），而同期在查一个投屏旋转问题
（「预览是正的、投到设备上反了」），用户要求：**先把圆形这部分代码都去掉，回到没加它之前的状态，
后面真用上了再加、再调试。**

⚠️ 记录一句备查：那轮改动本身**没有**动到旋转/导出——`preview.js` 全文不出现 `shapeType`，
导出画布、白底、`ctx` 调用一个字都没改，撤掉它不会改变投屏出图的任何行为。
撤的理由是「用不上的代码不留在主路径上」，不是「它就是元凶」。

## 撤了什么

| 文件 | 处理 |
|---|---|
| `subpackages/projection/preview/preview.wxml` | `.photo-wrap` / `.edit-clip` 上的 `{{device.shapeType === 1 ? 'is-round' : ''}}` 两处去掉，连同那段注释；回到单行写法 |
| `subpackages/projection/preview/preview.wxss` | 删掉 `.photo-wrap.is-round` / `.edit-clip.is-round` 两条 `border-radius: 50%` 及其注释 |
| `tests/projection-round-screen.test.js` | 删除（它守的就是上面那两条） |

撤法是**反向应用 `a6cd435` 的那两个文件补丁**（`git apply -R`），不是手改，所以
两个文件与 09-17 之前**逐字节一致**。全仓 `is-round` / `shapeType` 命中 0 处。

## 要加回来时怎么做

`a6cd435` 还在历史里，`git show a6cd435` 直接能拿到那两个文件的补丁和那份测试；
09-17 那份文档记着完整口径（预览裁圆、导出仍方、方形路径一个字不动）。
加回来时要一起解决的遗留问题也写在那份文档里：**框的比例来自设备后端 `width/height`，
圆形产品得先在后台把分辨率配成正方形，否则裁出来是椭圆**。

## 验证

`node tests/*.test.js` 全量 **59 个全部通过**（删掉圆形那个用例之后）。
