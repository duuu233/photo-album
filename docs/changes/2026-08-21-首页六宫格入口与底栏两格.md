# 首页下半部改成六宫格入口 + 底栏收回两格 + 换背景图 + 清理未引用图片（2026-08-21）

> 状态：**小程序端已实现**，**未真机验收**
> 最后核对：2026-08-21
> 适用范围：微信小程序首页（`pages/home`）、底部导航组件（`components/custom-tabbar`）、`assets/images`
> 设计稿：`assets/首页调整/首页-内容调整UI.jpg`（该目录已加进 `project.config.json` 的 `packOptions.ignore`，只留给设计走查，不进小程序包）
>
> ⚠️ 仅小程序，Flutter APP 待同步。

## 一、产品要求

1. 首页**下半部**换成设计稿那套入口（上半部：头像/Hi/设备轮播卡/指示点**一律不动**），背景图换成
   `https://oss.boltfox.cn/prodFile/202608211340094498724.jpg`；`assets/首页调整/` 里要用到的图标搬进 `assets/images` 并引用。
2. 底部 tabBar **只保留「首页」和「我的」**，另外两个模块（AI助手、官方图库）的入口都搬到首页。
3. 检查 `assets/images`，把项目里没用到的图片删掉，别让它们占小程序体积。
4. （追加）首页小标题**「选择投屏方式」也去掉**。

## 二、素材：白底 JPG → 透明 PNG

设计给的是 12 张**白底 JPG**（`home-icon01~06` 线稿图标、`home-icon11~16` 箭头徽标），命名规律是
「**第 N 项的图标是 `home-icon0N`，它配套的箭头徽标是 `home-icon1N`**」，颜色成对。
白底直接用不行：卡片是半透明白压在背景图上，图标会带一块白方块。故转成透明 PNG 落到 `assets/images`：

| 素材 | 做法 | 为什么不能反过来 |
| --- | --- | --- |
| 线稿图标 `01~06` | 整张白底掉透明：按「白底上的半透明墨」反解，`a = 1 − min(r,g,b)/255`，颜色 = `(P − 255(1−a))/a` | 图标**内部**也是白的（相机机身、文件夹内页），设计稿里那里透的是卡片底色，不是白 |
| 箭头徽标 `11~16` | 只掉**与画布边缘连通**的近白像素（四边泛洪） | 徽标的白色圆盘**要保留**；整张掉白会把圆盘一起吃掉，只剩一个箭头浮在卡上 |

转完再做一道**只压体积不改观感**的量化（`a≤8` 归零、颜色/alpha 取 4 级），把 JPEG 噪点压成大片同色，
12 张合计 74.2 KB → **46.5 KB**，肉眼对照无差别。

⚠️ 两处命名冲突，改名的是**旧**文件（新素材沿用设计给的名字，方便产品直接覆盖替换）：

- 旧 `home-icon02.png`（那张橙色设备缩略图）→ 更名 **`home-device-thumb.png`**。
  ⚠️ **这张图有六个引用点，不是一个**：首页设备卡、设备列表、设备详情、搜索设备（bind）、设备命名弹窗、投屏选设备弹层。
  当天第一版只改了首页，另外五处仍写着 `home-icon02.png` —— 文件还在、不报错，只是安静地换成了新的**蓝色相册图标**，
  「我的设备/设备详情」的设备图因此全变了样，属改名漏改。现已六处全部指回 `home-device-thumb.png`（文件内容与原来逐字节相同），
  并在 `tests/home-entries.test.js` 里钉死：六个引用点必须用 `home-device-thumb.png`，且**除首页 `HOME_ENTRIES` 外任何模板都不许再出现 `home-icon0N/1N`**；
- 旧 `home-icon05/06.png`（原来两张卡右下角的箭头）随改版下线，名字让给新素材。

⚠️ `home-icon04`（AI创作）设计稿最初给错了图（与 05 官方图库同一张），**产品当天已在 `assets/首页调整/` 里换成正确的魔法棒**，
本次用的就是换过之后那张。后续再要换图，**覆盖 `assets/images/home-icon04.png`（PNG、透明底）**即可，代码不用动。

## 三、首页下半部：六宫格入口

`pages/home/home.wxml` 里原来那两张卡（拍照 / 相册）整块换成 3×2 宫格，**上半部一个字没动**。
六项的图标/文案/主色集中在 `home.js` 的 `HOME_ENTRIES`，模板只 `wx:for` 一遍，不再逐张抄：

| # | 文案 | 副标题 | 进哪 | 闸 |
| --- | --- | --- | --- | --- |
| 1 | 拍照投屏 | 拍摄照片并投屏 | `tapCameraEntry` → 相机 | 登录 → 已绑定 → 已连接（`ensureCanProject`，含低电量提醒） |
| 2 | 相册投屏 | 选择照片并投屏 | `tapAlbumEntry` → 系统相册 | 同上 |
| 3 | 我的上传 | 管理我的照片 | `subpackages/album/list` | 登录（口径同「我的」页那一行） |
| 4 | AI创作 | AI生成精美图片 | `subpackages/ai/chat` | 无（与原底栏那一格保持一致，不借改版加闸） |
| 5 | 官方图库 | 海量精选美图 | `subpackages/gallery/list` | 无（同上） |
| 6 | 我的设备 | 管理我的设备 | `subpackages/device/list` | 复用 `goDeviceList`（自带连点锁） |

版式：卡宽 = 「750 − 两侧 48rpx − 两条 18rpx 列间距」三等分（≈206rpx，设计稿上量得 202rpx），
高 220rpx、圆角 28rpx，本体 `rgba(255,255,255,0.55)` + 毛玻璃 + 与设备卡同一套阴影——新背景是浅蓝白墙面，纯白卡会糊进背景里。

**卡内排布（当日二次调整，产品原话「有点太居左、上面图片和底部文案距离过大、有点丑」）**：

| | 调整前 | 调整后 | 为什么 |
| --- | --- | --- | --- |
| 卡高 / 纵向 | 250rpx + `justify-content: space-between` | **220rpx + 整组垂直居中**，图标与文案之间固定 20rpx | space-between 把图标顶到最上、文案压到最下，中间空出 68rpx 死档 |
| 左右内边距 | 16rpx | **左 20rpx / 右 12rpx** | 16rpx 时内容几乎贴着卡片左边缘；右侧是箭头徽标，它自带一圈透明阴影留白、视觉上不贴边，与设计稿量得的「左 18 / 右 12」同一套 |
| 图标 | 70rpx | 66rpx | 卡变矮后按比例收一档 |
| 副标题 | 17rpx | **18rpx**，颜色 `#9aa0a8` → `#8f959d` | 17rpx ≈ 8.5px 太吃力；文案列 130rpx，最长 7 字 × 18rpx = 126rpx 放得下 |
| 箭头徽标 | 40rpx，只对齐标题那一行 | **50rpx，垂直居中于「标题+副标题」两行整体** | 产品要求：徽标是对着右侧两行文案居中的，且要再大一点 |
| 标题 | 28rpx | 不变 | 与设计稿量得的 29rpx 基本一致 |

⚠️ 徽标的 **50rpx 是画布尺寸，不是观感尺寸**：素材画布 90×90 里白圆盘只占中间约 62，四周是透明阴影留白，
所以 50rpx 展示出来的圆盘 ≈ 34rpx（上一版 40rpx 的圆盘只有 ≈28rpx）。那圈留白本来就不可见，却实打实占着 flex 布局宽度，
故给它 `margin-right: -10rpx` 压进卡片右内边距——让出的 10rpx 还给左边文案列，副标题的字号才提得起来。

⚠️ **列间距不能只靠 flex `gap`**：老一点的安卓 WebView 不认，三张卡按 `(100%−36rpx)/3` 并排后右边会空出
36rpx、整行看着往左缩。现在 `justify-content: space-between` 与 `column-gap` 一起写（认 gap 的引擎上剩余空间恰好为 0，
两者结果相同），行间距则改由第一行三张卡的 `margin-bottom` 给——`row-gap` 同样不可靠。

**小标题「选择投屏方式」按追加要求删掉**：六项里已有四项与投屏无关，这句话把入口说窄了。
原来由它承担的上间距（已绑定 62rpx / 未绑定 136rpx）原样挪到 `.entry-grid` 自己身上，两种场景的位置观感不变。

## 四、背景图

`getHomeBgImage()` 换成 `https://oss.boltfox.cn/prodFile/202608211340094498724.jpg`（浅蓝墙面 + 窗光）。
`assets/images/home-bg-placeholder.jpg` 一并重做成**新背景的强压缩版**（2.3 KB）——它是首屏立刻铺的打底图，
与 OSS 大图**必须同源**，不同源就会看见一次「先旧底色、再换新底色」的跳变。

⚠️ **「我的」页的背景不换**（产品明确要求），它仍用原来那张 `202607310920402821453.png`。
但两页原本共用同一张占位图，首页换新之后「我的」页进页面会先闪一下新的浅蓝底色——
故新增 `assets/images/mine-bg-placeholder.jpg`（内容就是**原来那张**占位图），`mine.wxml` 改指向它。
一句话：**占位图必须与本页的大图同源**，两页背景不同就得各有一张。
（占位图由产品当时放在 `assets/首页调整/背景.jpg` 的原图压出来，那张与 OSS 上的文件字节数完全一致；原图之后已由产品删除。）

## 五、底栏收回两格

`components/custom-tabbar` 由四格（首页 / AI助手 / 官方图库 / 我的）回到**两格（首页 / 我的）**：
删掉 `goAi`/`goGallery` 两个方法与 `aiEntryEnabled`/`galleryEntryEnabled` 两个灰度开关，栅格 `repeat(4,1fr)` → `repeat(2,1fr)`。
AI 与官方图库本来就不是 tab 页（各自在分包里、靠 `navigateTo` 进），所以只是入口位置变了，页面一行没改。

⚠️ **「接着上次那一页」的 AI 续聊规则（2026-08-13 需求 5）没丢**，跟着入口搬到了 `home.js` 的 `goAi`：
上次停在某条会话里就带 `sessionId+title` 回那条会话，停在默认页就还是默认页。
`tests/ai-last-session.test.js` 的第 ② 段随之改成驱动首页的 `tapHomeEntry('ai')`，其余三段（记忆语义、聊天页两个时刻记自己、删会话清记忆）原样不动。

## 六、清理 `assets/images` 未引用图片

按「文件名在全部 `.js/.wxml/.wxss/.json/.wxs` 里搜不到就是没用」扫了一遍，删掉 **20 个**：

- **本次改版才空出来的**：`home-camera-card-bg.png`、`home-album-card-bg.png`（旧两张卡的底图，各 22 KB）、
  `camera_material.png`、`album_material.png`（旧卡插画）、`ai-tab.png`、`ai-tab-active.png`、`tab-gallery.png`、`tab-gallery-active.png`（底栏中间两格的图标）；
- **早就没人引用的历史遗留**：`ai-chat.png`、`ai-spark-white.png`、`ai-loading-bubble.png`（加载气泡早已改成纯 CSS）、
  `gallery-fav-badge.png`、`mine-icon-favorite.png`（收藏图标已统一成 `heart-solid/outline`）、`mine-icon08.png`、
  `device-list-icon04.png`、`device-list-icon05.png`、`preview-icon03-selected.png`、`return-arrow-icon.png`、`return-round-icon.png`、
  `pay-paypal.png`（PayPal 通道未接入、选项在 wxml 里隐藏着）。

⚠️ 判据只认**运行时引用**：`ai-loading-bubble.png` / `gallery-fav-badge.png` 这几张在注释里还被提到（讲当年为什么换掉），
但没有任何 `src` 指向它们。要恢复任一入口（如 PayPal、底栏图库格）时，从 git 历史取回图片即可，代码里没有指向空文件的引用。
⚠️ `assets/images/BatteryLevel/*` 是 `utils/battery.js` 里 `battery-${n}.png` **拼出来的动态路径**，扫描时必须整目录豁免，不能当没引用删掉。

顺手删掉了 `home.wxss` 里早已没有对应节点的「拍照/相册二选一」弹层样式（`.media-sheet` 整块）。

**体积账**：`assets/images` **841.4 KB → 725.2 KB（−116.2 KB）**，这还是**已经加进 12 张新图标（46.5 KB）之后**的净值；
`assets/首页调整/`（设计稿 + 原始 JPG 素材）已加进 `packOptions.ignore`，留在仓库里但不进小程序包。

## 七、测试

- `tests/home-entries.test.js`（新增）：六项的顺序/文案、`home-icon0N`↔`home-icon1N` 的命名规律、**12 个素材文件真实存在**
  （引用一张不存在的图小程序不报错、只渲染成空白，非测不可）、六项各自跳去哪、背景图换成新 URL、小标题已删、底栏只剩两格且按两列分。
- `tests/ai-last-session.test.js`（改）：AI 续聊入口由底栏改到首页六宫格，规则断言不变。
- 既有用例全部通过（53 个文件）。

## 八、待办

- ⚠️ **真机未验**：宫格版式（尤其 4 字标题 + 箭头在 206rpx 卡内的余量）、毛玻璃在低端安卓上的表现、新背景图的观感都只在代码层面完成。
- Flutter APP 侧未同步（首页入口、底栏格数、背景图）。
- 副标题按设计稿只有 17rpx（≈8.5px），比小程序常见的最小字号还小一档；真机上若觉得吃力，调 `.entry-desc` 一处即可
  （文案列宽 126rpx，17rpx × 7 字刚好放下，字号往上调要同步收窄标题或箭头）。
