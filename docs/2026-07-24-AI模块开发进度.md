# AI 对话模块（星宝）开发进度

## 操作日志（最新在上）
- **2026-07-25（二）对接 API 文档 v1.0.3 → v1.0.4**（`assets/ai/BoltStar-API-Doc-v2-1.0.4.md`）。
  文档四处功能变更，逐条落地：
  1. **`new_session` 参数已废弃**（§三/§四/§十）——会话是不是新的改由后端自己判断。
     `utils/ai-api.js` `chat()` 删掉 `if (params.newSession) data.new_session = true` 分支及入参说明。
     - 实际影响为零：本项目从来没有调用方传过 `newSession`（`chat.js _dispatchChat` 只传
       sessionId/message/imgOrientation/imgStyle/imageUrls），所以这条属于**清理死代码**，运行行为不变。
     - ⚠️ 别和 `sessions.js` 里的 `wx.setStorageSync('aiOpenSession', { newSession: true })` 搞混——
       那是「跨页告诉聊天页开个新会话」的**本地标记**，与接口参数同名但毫无关系，**保持不动**。
  2. **「你好，我是星宝✨」接口不再返回**，明确为前端静态展示（§二/§四）。
     本项目本来就是静态的（`chat.wxml` 的 `.welcome` 区块，仅 `messages` 为空时显示），**无需改**，
     只在 `ai-api.js chat()` 注释里记下「text 不会再带招呼语前缀」，免得以后有人再去剥前缀。
  3. **新错误码 `20013 MAX_SESSIONS_REACHED`**（每用户上限 **20** 个会话，`POST /session/new` 返回）：
     - `utils/ai-i18n.js` + `assets/ai/BoltStar-i18n-Errors.json` 四语种补文案；
     - `handleAiError` 加 20013 分支：文档要求「引导用户清理旧会话」，光 toast 用户不知道去哪清，
       故页面传了 `onSessionLimit` 就弹「去清理/取消」确认框（没传则退回 toast，与 30xxx 的 `onRetry` 同套路）。
       重试对它没意义，所以不落进重试分支。
     - `chat.js createSession()` 传 `onSessionLimit: () => this.goSessions()`，直接把人送到会话列表页删。
     - `sessions.js createNew()` 再加一道本地前置拦截（`MAX_SESSIONS = 20`）：本页已握着完整列表，
       达上限就地 toast，省掉一次注定失败的往返，用户也正好在能删的页面上。
  4. **`session.title` 规则明确为「首条用户消息前 20 字，默认『新对话』」**：`chat.js` 本地同步标题时
     按 `SESSION_TITLE_MAX = 20` 截断，避免列表页重拉后标题突然变短、两处对不上。
  - 关联文件：`utils/ai-api.js`、`utils/ai-i18n.js`、`assets/ai/BoltStar-i18n-Errors.json`、
    `subpackages/ai/chat/chat.js`、`subpackages/ai/sessions/sessions.js`。
  - 📄 文档小瑕疵（不影响代码）：v1.0.4 §十二「其他注意事项」第一条被截断成乱码
    （`违规�cope-DataInspection...`），原意见 v1.0.3 同条。
  - ⚠️ 未真机验证（20013 需要造满 20 个会话才能触发）。**Flutter APP 未同步**本轮四条，
    对应落点为 `boltstar_ai_api.dart` / `ai_i18n.dart` / `ai_{chat,sessions}_page.dart`。
- **2026-07-25 Flutter APP 已同步本模块**（`/home/pg/dh/flutter`）。落点：`lib/src/network/boltstar_ai_api.dart`
  （接口层，独立于 BoltFox 的 ApiClient；`AiCall.abort()` 用 http.Client.close 实现「停止生成」）、
  `lib/src/features/ai/ai_i18n.dart`（四语种错误码表 + 按区间分发 toast/封禁弹窗/重试框）、
  `lib/src/features/ai/presentation/ai_{chat,sessions}_page.dart`、`lib/src/features/ai/ai_entry.dart`
  （入口开关 `kAiEntryEnabled=false` + 暗门口令，与小程序 custom-tabbar 屏蔽状态一致）、
  调试台 `ble_debug_page.dart` 底部暗门。App 侧同样走**非流式 + 客户端打字机**（本可走 SSE，先与小程序保持同链路）。
  - ⚠️ **两处有意差异**（App 无对应能力，未擅自加依赖）：① 语音输入只给占位提示，不录音（小程序会录音、
    只是转文字插件未开通）；② 「下载」只落到应用缓存目录——Flutter 无内置写系统相册能力，需 gal /
    image_gallery_saver 之类插件。要补齐这两项请先确认可以加依赖。
  - ⚠️ 「去绑定」：App 的绑定流程是首页内嵌浮层、无独立路由，只能退回上一页由用户在首页绑定。
  - ⚠️ 调试台在 App 里仅 `kDebugMode` 可达 → **release 包中 AI 页暂无任何入口**（小程序调试台正式包仍可达）。
  - ⚠️ 本机无 Flutter SDK，`dart analyze` / 真机均未跑。详见 flutter 侧 `docs/app-vs-miniprogram-sync.md` 07-25 条。


- **2026-07-25 对接 API 文档 v1.0.2 → v1.0.3**（`assets/ai/BoltStar-API-Doc-v2-1.0.3.md`）。前端相关变更与落地：
  - **`/chat` 新增 `image_urls`（图文多模态，最多 4 张）**：`utils/ai-api.js` `chat()` 带上 `image_urls`；服务端按
    「张数 + 生图关键词」自行分流（1 张+关键词=图生图美化 / 多张+关键词=友好拒绝 / 其余=分析讨论），前端不判关键词。
  - **输入框内选图（§5.1.4）**：改为「缩略图停在输入框内、每张可删、最多 4 张、纯图片不可发（须配文字）」。
    `chat.js` 用 `pendingImages` 数组承载：相册可多选（`media.chooseFromAlbum(remain)`）、选后**立即上传 OSS**
    并显示上传中遮罩、回填 URL；点发送时随文字一起走 `/chat`（`onSendTap`→`sendChat(text,undefined,urls)`）。
    **旧「选图后下一条输入 → `/image/enhance`」单图链路下线**（`sendEnhance`/`_pendingEnhanceUrl` 删除；
    `enhanceImage` 接口保留作独立入口）。发送逻辑抽出 `_dispatchChat` 使 30xxx 重试只重发不重复堆用户气泡。
  - **新错误码 `20012 INVALID_IMAGE_COUNT`**：`ai-i18n.js` + `BoltStar-i18n-Errors.json` 四语种补「一次最多处理 4 张图片」；
    前端在选图时已按 4 张客户端拦截（toast「一次最多选择 4 张图片」），双保险。
  - 关联文件：`utils/ai-api.js`、`utils/ai-i18n.js`、`utils/media.js`、`subpackages/ai/chat/{js,wxml,wxss}`、`assets/ai/BoltStar-i18n-Errors.json`。
  - ⚠️ **文档 v1.0.3 自身不一致，已按主流写法取舍**：① 目录列了「八链式编辑/九被动学习」但正文无此两章，
    且正文 `参数速查表/错误码/已知问题` 仍是「七/八/九」未跟着改号（正文与目录冲突）；② §5.1.4 状态机与用户旅程图都写
    「图片+文本 → `POST /chat`」，但同节「API 调用」示例又写 `POST /image/enhance`——**取状态机/旅程图/§二 的
    `/chat`+`image_urls` 为准**（它才覆盖多图讨论/拒绝等全部 6 种场景，enhance 单接口做不到）。真机联调需回归确认。
  - **Skill 系统（新增 §七）** 属服务端上下文引擎，「记住偏好/忘记偏好」就是普通聊天文本，**前端无需改**（照常发 `/chat`）。
- 2026-07-24 首版 demo 完成：AI 聊天页 + 会话列表页 + BoltStar 接口层 + 四语种错误文案；tabbar 中间加「AI生图」入口；真机接口冒烟通过（session/new → chat → history → delete 全链路 OK）。关联文件见下「文件清单」。

## 需求与依据

| 材料 | 用途 |
|------|------|
| `assets/ai/支付&ai&官方图库.docx` | 新版需求（AI对话/支付体系/官方图库三模块），本轮只做 AI 模块 |
| `assets/ai/BoltStar-API-Doc-v2-1.0.4.md` | 第三方 AI 接口文档（阿里云 FC）**当前对接版本**；v1.0.3 为上一版，保留供 diff |
| `assets/ai/BoltStar-Error-Codes.md` | 错误码说明 |
| `assets/ai/BoltStar-i18n-Errors.json` | 四语种错误文案（zh-CN/zh-TW/en/ja），按当前语种弹提示 |

UI 未出图：**所有图标用色块占位**（`icon-block` 系列样式），UI 出图后把色块换成 `<image>` 即可，逻辑不动。

## 文件清单

| 文件 | 职责 |
|------|------|
| `utils/ai-api.js` | BoltStar 接口层：session/new、session/list、DELETE session、chat/history(GET/DELETE/clear)、chat、image/enhance。统一 reject `{code, params, detail}`；chat/enhance 的 promise 带 `.abort()`（停止生成用） |
| `utils/ai-i18n.js` | 四语种错误文案（与 BoltStar-i18n-Errors.json 一致）+ `handleAiError` 按 code 区间分发（toast/封禁弹窗/重试确认框）+ 一键生图多语种 message 拼接 |
| `subpackages/ai/chat/*` | 聊天主页面 |
| `subpackages/ai/sessions/*` | 会话列表页 |
| `components/custom-tabbar/*` | 中间新增凸起「AI生图」按钮（navigateTo 进聊天页，非 tab 页） |
| `app.json` | 注册 `subpackages/ai` 分包并加入首页 preloadRule |

## 已实现功能

聊天页（chat）：
- 星宝招呼语默认态（需求原文案）；消息气泡流（用户右/AI 左）
- AI 回复：占位气泡三点 loading → **客户端打字机 30ms/字**（小程序不支持 SSE，走非流式 URL，文档 §四/§6.4）
- 生成图片以图片气泡展示，点击预览，`lazy-load`
- 「停止生成」浮标：中断 wx.request（`requestTask.abort`）+ 结束打字机，已打出内容保留
- 输入区：文本输入+发送；🎤 切换「按住说话」（录音已接 RecorderManager，**转文字待开通同声传译插件**，先占位提示）
- ＋工具面板：相册 / 拍照 / 一键生图（漫画·风景·肖像·动漫 → img_style）/ 图片比例（竖·横·方 → img_orientation，必传，默认竖向）
- **图文多模态（v1.0.3，2026-07-25 改）**：相册/拍照选图（相册可多选，最多 4 张）→ `api.setFileUpload` 上传拿 URL →
  缩略图停在**输入框内**（每张可删、上传中转圈、纯图片不可发）→ 输入文字点发送，随 `image_urls` 一起走 `/chat`，
  服务端按张数+关键词分流（图生图/多图讨论/友好拒绝）。〔旧「下一条输入=`/image/enhance`」单图链路已下线〕
- 长按消息：文本→删除；图片→下载（存相册，拒权引导设置）/ 投屏 / 删除
- 投屏：已连接活动设备→直接进投屏；未连接→弹已绑定设备列表→选中后走投屏。实现方式：AI 图下载成临时文件，包装成与手选照片同形态的 `pendingProjection` 跳 `projection/preview`，预热连接/自动连接/图传全部复用现有链路
- 会话标题：首条消息后本地同步为消息内容（与后端 title 行为一致）
- Token 余额展示 + 每次成功对话扣 1 + 余额不足弹窗拦截（**本地 demo 模拟**，Java 后端支付体系未接）
- 无绑定设备拦截（API 文档 §5.5）：进页检查，未绑定弹「去绑定」
- 封禁（22002/22003）：弹窗 + 禁用输入 + 顶部横幅

会话列表页（sessions）：
- 时间倒序；标题=首条消息；今天显示 HH:mm，其余 MM-DD HH:mm
- 分页每页 20 条滚动加载（接口一次回全部，前端分段展示）
- 长按删除单条（需求"左滑或长按"取长按）；清空全部二次确认（接口无批量删，逐条删）
- 新建会话；删除/清空当前打开的会话时通知聊天页重置
- 「仅保留最近 7 天」提示；历史过期 code=10001 → toast `info.10001`

错误处理（全模块统一走 `ai-i18n.handleAiError`）：
- 文案按 `utils/language.getSelectedLanguage()` 取对应语种（zh-Hans/zh-Hant/en/ja），`{hours}` 等占位替换
- 2xxxx 参数/违规 → toast（带「接口-」来源前缀，沿用项目 toast 约定）；22002/22003 封禁 → 弹窗+禁用聊天；3xxxx 上游错误 → 弹「重试」确认框（有重试回调时）；未知 code 降级 31001
- `detail` 是排障信息只打 console，**严禁展示给用户**（文档红线）

## 关键决策记录

- **小程序只能走非流式** `boltstaat-agent-fwdomalzks...fcapp.run`（wx.request 无 ReadableStream），流式 URL 仅存常量备 Web/Flutter 用
- **user_id 规则**：`boltfox_<用户userNo>`，未登录兜底 `boltfox_demo_user`（见 ai-api.getAiUserId；同一 user_id 多端历史互通）
- **独立接口层不复用 request.js**：第三方 FC 的 Base URL/响应结构/错误码体系与 BoltFox 后端完全不同，公共参数注入也不适用
- chat/enhance 超时 120s（生图上游可能 30s+），普通接口 15s，均不重试（生图 POST 重试有重复扣费风险）

## 冒烟验证（2026-07-24，curl 直连）

`POST /session/new` → 10000 ✅；`POST /chat`（你好+vertical）→ 10000 返回星宝自我介绍 ✅；`GET /chat/history` → 消息带 id/role/content ✅；`DELETE /session` → 10000 ✅。响应结构与文档一致。

## 待办 / 残留

1. **同声传译插件**：小程序后台开通 WechatSI 后，把 chat.js `initRecorder` 的占位 toast 换成 转文字→直接 sendChat（文档 §5.1.2：按住说话松手直发）
2. **Token 支付体系**：Java 后端接口就绪后替换 chat.js 的 `readTokenBalance/spendToken` 本地模拟；「Token管理」页（个人中心入口/套餐/购买&消费记录）属支付模块，未开发
3. **当轮新消息删除只是本地删**：`/chat` 响应不带 message_id，重进会话会重现；待后端在 /chat 响应回 message_id 或前端发送后静默重拉 history 对齐
4. **UI 出图后**：替换全部 `icon-block` 色块占位（chat/sessions/custom-tabbar）
5. **官方图库模块**：未开发（需求文档第三部分）
6. **认证**：BoltStar 暂无鉴权，后续升级 JWT 时在 ai-api.js 统一加 header
7. **Flutter 未同步**：整个 AI 模块小程序先行
8. 图片 URL 24h 过期：下载/投屏失败提示已带说明；再次投屏旧图需重新生成
