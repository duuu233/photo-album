# 项目知识地图

> 状态：current
> 最后核对：2026-08-13
> 适用范围：`photo-album` 微信小程序及直接相关的接口、BLE/OTA 协议和跨端协作

本目录保存所有项目文档型 Markdown。仓库根目录只保留 `AGENTS.md`，因为它是代理工具自动发现的项目指令文件，不属于普通项目文档。

## 当前长期知识

### Architecture

| 文档 | 职责 |
| --- | --- |
| [设备身份与 BLE 会话](architecture/device-identity-and-connection.md) | 完整 6 字节设备 ID、广播候选、BLE 临时句柄、会话认领和重连不变量 |
| [图片投屏流水线](architecture/image-projection-pipeline.md) | 预览、seekink 出帧、后端建记录、BLE 图传、记账与性能并行 |
| [照片预览与非破坏性编辑](architecture/photo-preview-editing.md) | 常驻编辑、按张状态、横竖向取景、设备动态角度导出和缓存策略 |
| [AI 客户端架构](architecture/ai-client.md) | BoltStar 客户端边界、会话、消息图片、错误处理和未完成能力 |

### Protocols

| 文档 | 职责 |
| --- | --- |
| [OTA / DFU 协议](protocols/ota-dfu.md) | FF10/FF11、F1/F2/F3/FC 帧、结果码、尾包与成功判定 |

### Decisions

| 文档 | 职责 |
| --- | --- |
| [设备图片槽位索引](decisions/image-slot-index.md) | `imgIndex` 物理身份、0 号位、幽灵记录和失败一致性 |
| [电量缓存与平滑刷新](decisions/battery-cache-and-refresh.md) | 15 秒 TTL、在途去重、旧值平滑展示和禁止假值兜底 |

### Reference

| 文档 | 职责 |
| --- | --- |
| [客户端接口矩阵](reference/client-api-matrix.md) | BoltFox 客户端接口、平台适用性和当前接入状态 |
| [BoltStar AI 参考文档](reference/ai/README.md) | 当前/历史 API 版本与错误码草案入口 |

## 变更记录

- [2026-08-13 OTA 收尾改「蓝牙栈硬复位」+ 升级页闪帧、图库切分类、星币表头、AI 入口续聊](changes/2026-08-13-OTA连接硬复位与AI入口续聊.md) — 仅小程序，**Flutter 待同步**（第 1 条是通用连接不变量、第 5 条是路由口径）；⚠️ 真机未验。第 1 条是 08-12 那篇的**续修**：判据没错（无主连接占着设备使其不广播），但手段够不着 —— `releaseStrayConnections` 依赖的 `getConnectedBluetoothDevices` **按「已发现的服务」筛设备**，服务发现没走完的半连接、设备复位途中微信还扣着的连接对象它一条都报不出来，而 OTA 收尾这一刻的连接全是这一类。改为直接用「重进小程序」的等价物 `closeBluetoothAdapter → openBluetoothAdapter`（新增 `device-ble.resetBluetoothStack`），调用点两处：**一轮 OTA 收尾**（`upgradeFirmware` **await 到它落地才返回**，否则页面已显示「升级成功」而适配器还在关）与**冷连接前**（仅当没有任何会话可失去时）；升级中途的 wedged 重试显式 `hardReset:false`（那条路紧接着要用同一个适配器重连）。⚠️ 三个坑已锁进单测：复位前**必须拆掉 `bluetooth.js` 的共享扫描会话**（不拆的话新扫描会订阅到一条收不到广播的死会话，等于没修）、监听重挂**必须先 off 再 on**（`wx.onBLE*` 是累加注册，重复注册＝每帧派发两遍、解帧缓冲当场废掉）、**没碰过设备的失败不许复位**。另四项：升级页 `auto=1` 直接落「进行中」画面（此前先渲染一帧「发现新版本」，底部按钮区闪一下 —— 只关 `showPrimary` 不够，「返回」和主按钮在同一块 `wx:if`）；官方图库切分类**先清屏再拉**（此前旧图与「加载中」同屏，`onShow` 那次同步收藏态改静默刷新、不清屏）；星币规则表头「星币消耗（星币/次）」→「星币/次」（原文在 26% 宽的列里必然折行拆开括号）；底栏 AI 图标**接着上次那一页**（新增 `utils/ai-last-session.js`，判据就是聊天页 `sessionId`：有会话回会话、空态回默认页；**只存内存**——会话历史服务端只留 7 天，跨启动记住可能已过期/已删的会话只会给用户一个报错的空页；`onShow` 与 `onUnload` 都记，且 `onUnload` 只在「栈里没有别的聊天页」时记，两者触发顺序在各基础库上并不一致）
- [2026-08-12 重绑带出后端昵称、未绑定弹窗改自绘、星币入口加箭头、新增星币消耗规则表](changes/2026-08-12-重绑带出昵称与星币消耗规则.md) — 仅小程序（**第 4 条星币消耗规则已同步 Flutter**，其余待同步）；`addUserProduct` 现在会把**解绑前的旧昵称**回带，命名弹窗要带出它 ——⚠️ 两处一起改才生效（本地拼的 `productName` 原来盖在 `saved` 后面；且 `normalizeDevice` 的 `name` 取「第一个非空」，蓝牙广播名排在 `productName` 前面）；AI 页「未绑定设备」提示由 `wx.showModal` 改自绘（**这个遮罩不做点外关闭**，它是「去绑定/返回」的二选一）；会话列表星币胶囊加 `>` 并可点进星币管理；星币管理页新增「星币消耗规则」表（`GET /Client/Order/getAiConfigList`，`retData` 是**裸数组**，顺序原样按后端权重来，`30.0` 显示成 `30`，拉不到整块不渲染）
- [2026-08-12 投屏结果页「投屏明细」改进我的相册 + 确认购买页支付中全屏 loading](changes/2026-08-12-投屏明细入口与支付全屏loading.md) — 第 1 条**双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-12-AI星币闸与文案同步.md`，未编译未真机），第 2 条 App 不适用（无购买页）；结果页那一行改进 `subpackages/album/list`（刚投完想看的是「那几张现在长什么样」），行文案与 `n/n` 计数不动。⚠️ **副作用：投屏记录页（失败记录/重新投屏）自此全项目无入口**，代码保留、待产品指定挂哪；确认购买页在 `paying` 期间盖全屏 loading —— 这条链路最长约 10s（建单→拉起支付→**到账轮询 9.4s**），此前中间那段没有任何反馈，文案按 `purchase` 新增的 `onStage` 分三阶段换（下单/拉起支付/确认到账），`addOrder` 的原生「下单中」在这条路上关掉，免得两个转圈叠一起
- [2026-08-12 官方图库由 mock 切到真实后端（`/Client/Product/*`）](changes/2026-08-12-图库接口对接.md) — **双端已同步**（2026-08-12 App 侧从零补齐该模块并接同样五个接口，见 `flutter/docs/history/2026-08/2026-08-12-补齐官方图库与星币模块.md`；⚠️ App 未编译未真机），⚠️ 真机未验；分类/列表/详情/收藏切换/收藏列表五个接口全部接入，`utils/gallery-api.js` 重写但**对页面的字段名不变**（wxml 基本没动）。三处易接错已锁进单测：`productImgId` 是 integer（页面比对/进 URL 要字符串、提交要数字）、详情简介叫 `content`、收藏态是 `isAlreadyCollected` **0/1 不是布尔**；列表是分页壳，判停优先 `pageCount`。页面随之改：**上拉续页**（续页是往两列追加，不能重新分列，否则用户正看的卡片会被挪走）、端上补一个 id 为空串的「全部」分类（后端没有，列表接口 `categoryId` 可选）。⚠️ **两处后端缺口端上在兜**：列表项**没有图片比例**（先按 3:4 占位、`bindload` 后校正那一张）、列表项**没有收藏态**（另拉一页收藏列表在端上标记）—— 请后端补 `width/height` 与 `isAlreadyCollected`；另 `setImgCollected` 返回的布尔语义未定，端上一律按取反推新状态
- [2026-08-12 官方图库三项：收藏入口移到分类条右端、顶部导航收紧、图片详情让大图占更多](changes/2026-08-12-图库收藏入口与详情版式.md) — **双端已同步**（App 侧当天补齐官方图库模块，这三条口径在新页面里一次到位；🔶 入口有意不同：App 挂在「我的 → 服务与帮助」）；「我的收藏」从「我的」页移到官方图库分类条**右端固定位**（收藏的就是图库的图，入口贴着图库更顺手；「我的」页同时少打一次 `getFavoriteCount`）——⚠️ 收藏按钮必须是 `scroll-view` 的兄弟节点、`.category-scroll` 必须 `min-width:0`，否则不是跟着分类滑走、就是被分类挤出屏幕；`page-nav` 新增 opt-in 的 `compact`（标题小一号 + 收掉行下留白，**导航行本身不能压**，它必须与微信原生胶囊同高同轴）；图片详情去掉「适用设备尺寸」整块，大图 46vh→58vh、白卡起点 43vh→55vh，⚠️ 让开贴底按钮的 `padding-bottom` 原挂在被删的 `.detail-sizes` 上，已移到 `.detail-card`
- [2026-08-12 AI 模块五项调整：`/chat` 带 `usertoken`、本地图片放宽到 5 张、Token 文案改「星币」、对话前服务端校验](changes/2026-08-12-AI模块五项调整.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-12-AI星币闸与文案同步.md`；⚠️ App 未编译未真机，且无购买页故弹窗只有「知道了」）；`/chat` 新增全小写 `usertoken`（值是登录接口下发的 `userToken`，**不是** `Authentication` 头里的 `jwtToken`，接错了不报错只会静默认不出用户）；上传上限 4→5 张并统一提示「当前AI只允许上传5张图」（⚠️ BoltStar 文档 §二仍写 4 张，需与 AI 侧确认已放宽）；面向用户的「Token」一律改称「星币」（代码标识符与后端字段名不动，调试台的鉴权「第三方 Token」不在此列）；新增 `GET /Client/Order/chkAiDialogue` 作为**发起对话前的唯一闸**——够不够扣由服务端裁决，端上自判余额的 `LIMIT_ENABLED`/`hasBalance`/`guardToken` 一并删除，拦下时 `/chat` 不发、气泡不上屏、**草稿还回输入框**，提示「至少需要 30 星币」的数字取自后端、话由端上说；⚠️ 真机实测**「不够」的答复是 `retCode=403`+`retMsg`、不是 200+false**（走 request.js 的失败分支，只认 `retData===true` 会把它当成接口异常而放行）；除 403 外的失败一律放行（读不到判据就锁死 AI 是更糟的失败模式）
- [2026-08-12 OTA 升级失败/中断不再拖垮设备的连接与读取](changes/2026-08-12-OTA失败中断不再拖垮设备连接.md) — 仅小程序，**Flutter 待同步**；现场「升级失败设备就连不上，退出小程序重进又正常」——「重进就好」本身是判据：storage 缓存（直连句柄/身份登记表/选中设备）跨重启保留，重进只清内存会话与**微信持有的全部 BLE 连接**，故障必然是**一条还连着、账本上却没有的无主连接**占着设备（单连接设备被占时不广播，且 `disconnectAll` 按账本遍历、谁也关不掉）。根因：`scanMatchConnect` 连上候选后**只有身份不匹配才断链**，而 OTA 刚失败时撞上的恰恰是 `0x01` 读超时那一种。改法：连上后任何失败一律断链 + 新增 `releaseStrayConnections()` 在扫描前对账释放无主连接（把「重启小程序」做进连接主链）。顺带把收尾力度按「碰没碰过设备」分级：包下载/解析失败不再断掉用户已有连接、也不扣「电子纸设备-」前缀；**中断（退出升级页）改走与失败同一套完整收尾**（此前只断 OTA 会话，FF00 会话/0x01 读数/电量缓存全留着 → 详情页拿死链路一直超时）
- [2026-08-12 详情页「固件升级」右侧改为版本比对结论：有版本可更新 / 已是最新版本](changes/2026-08-12-固件升级行改版本比对结论.md) — 仅小程序，**Flutter 待同步**；右侧原本显示版本号本身（`newVersionNo || firmwareVersion`，两个字段一个是云端最新、一个是设备当前，用户看到数字却无从判断该不该升级），现改为比对结论：**设备实际在跑的固件版本（调试台那条路，`0x03 GET_SW_VER`）与接口 `newVersionNo` 不同、且 `downloadPath` 是有效 `.bin`** → 「有版本可更新」，反之「已是最新版本」；读不到当前版本（未连接/`0x03` 失败）回落 `--`，不拿「已是最新」骗用户不去升级。判定收敛成 `evaluateFirmwareUpdate` 一处，**点击流程与 `ota.js` 一并同源**（否则详情说可更新、升级页却因后端 `isUpdate=0` 说已是最新，用户永远升不了级），仅在读不到设备版本时退回 `isUpdate` 标志以保住「不连蓝牙也能查版本」。⚠️ 真机未验：两侧版本号写法必须可比
- [2026-08-11 OTA 后「设备ID 从 4 位变 6 位就连不上」：连接链路两种长度都兼容 + OTA 全链路日志](changes/2026-08-11-OTA后设备ID四六字节兼容与全链路日志.md) — 仅小程序，**Flutter 待同步**；固件把广播 Device_ID 从 4 字节换成 6 字节后正式入口连不上（调试台"能连"是假象，它 `allowAll` 扫描 + 按句柄直连，把白名单、候选匹配、`0x01` 校验三道闸全绕过了）。根因三处：① 广播 6 字节与 `0x01` 写法不同时被 `serialsMatch`「长度相同却不相等」一票否决，**候选一个都筛不出**；② 广播布局与 08-04 的假设不符时整条解析作废，设备在列表里连屏型/电量都没有；③ 广播 6 字节现在长得像完整身份，**能越权认领活动会话**（明文可伪造，OTA 打错设备不可逆）。改法：解析优选布局解不通才回退、扫描白名单按厂商数据放行、候选匹配强弱两轮 + 同型号兜底候选（身份闸不动，仍由 `0x01` 精确校验裁决）、会话认领只认 `0x01` 核实过的 ID。另按要求给 OTA 加了 `[OTA#n +x.xxs]` 全链路日志（包/连接/START/头信息/每个 PRN 窗口/END/收发原文）
- [2026-08-11 OTA：删除整套测试升级模块 + 修好「点了没反应」的真实升级](changes/2026-08-11-OTA删除测试模块与真实升级修复.md) — **协议层修复与「连接并升级」双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-11-OTA真机修复同步.md`；⚠️ App 侧未编译未真机验证，且「删除整套测试升级模块(干跑/mock)」这一项 **Flutter 仍待同步**）；删掉「OTA测试升级」行、「固件升级」长按测试、`test=1`/`dry=1`、`dryRunFirmware`/`mockFirmwareBuffer`（干跑让「升级成功」有了两种含义）。真实升级的「点了没反应」根因是**按钮画得出来、`canUpgrade` 却因未连接恒 false**：现改为按钮「连接并升级」+ 升级前就地自动扫连，所有分支都不再静默 return
- [2026-08-11 AI 模块 Token 接口对账：余额由本地 demo 切到真实账户](changes/2026-08-11-AI与Token接口对账.md) — 仅小程序，Flutter 待同步；按 https://api.boltfox.cn/swagger-ui.html#/ 逐条核对 Token 五个接口：登录/用户信息**新增出参 `availableToken`** 已打通（「我的」页不再多打一次 `getUserAccount`，`null`≠`0`），AI 模块余额由假的 100 切成真实账户值且**端上不再自扣**（swagger 无消费端点，扣费在服务端），支付查单 `getWxVirtualPayQueryOrder` **在文档里已不存在**→改调 `getPayQuery`（`payState`）；08-08 记的 `addOrder` 缺 `signData/paySig/signature` 缺口**已被后端补齐**
- [2026-08-10 「我的相册」删除跳过设备上已经没有的槽位](changes/2026-08-10-相册删除跳过设备已无的槽位.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-11-相册删除跳过空槽位与AI八项优化.md`；App 侧顺带修掉 `deleteImage` **吞掉失败结果码**——固件回 0x04/0x07/0x09 时当成删除成功、后端记录照删）；多手机共用一台设备时，另一端删过的那张在本机列表里还在，空槽位一起下发 `0x12` 会被固件回 `0x07` 打回**整批**（提示「掩码不一致(该位置已有图/索引越界)」）。改为先读 `IMG_MASK` 分拨、空槽位跳过、`0x05`/`0x07` 放行，后端记录一律删掉；顺带修好 08-10 上午起就跑不通的 `tests/album-slot-index.test.js`
- [2026-08-10 AI 助手八项优化（顶栏标题居中、会话入口、Token 位让出等）](changes/2026-08-10-AI助手八项优化.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-11-相册删除跳过空槽位与AI八项优化.md`；⚠️ 第 5 项顶栏排布两端有意不同：APP 的会话列表按钮在最右）；聊天页不再渲染 `custom-tabbar`，顶栏改标题居中 + 返回/会话两颗按钮，Token 展示位从右上角让出
- [2026-08-08 微信虚拟支付对接（Token 由 mock 切真实后端）](changes/2026-08-08-微信虚拟支付对接.md) — 仅小程序；接入 `/Client/Order/*` 与 `/Client/Pay/getPayQuery`（原 `getWxVirtualPayQueryOrder` 已下线），新增 `utils/wx-virtual-pay.js`；到账判据是**服务端余额变多**（退避轮询 ~9.4s）而非支付 success 回调。~~⚠️ `addOrder` 出参仍缺 `signData`/`paySig`/`signature`~~（2026-08-11 核对：后端已补齐）。**2026-08-12 补「iOS 端门槛」一节**：真机 `-15001 INVALID_PLATFORM` 与参数/签名/开通都无关，是**微信客户端 8.0.59 < 8.0.68**——小程序内的 iOS 同样调 `wx.requestVirtualPayment`（微信路由到苹果 IAP 通道），但要同时满足开通 IAP／**已配小程序简称**／iOS 15+／微信 8.0.68+／**不支持沙箱(env=0)**／Apple ID 大陆区／单笔 ≥1 元七条。端上据此在 `addOrder` **之前**拦住并提示用户升级微信（不留永远付不掉的死单），`-15001` 不再裸奔给用户；排查日志按 **errMsg 优先**给提示——`-15001` 是个筐，`INVALID_PLATFORM` 与 `PRODUCT_ID_EMPTY` 是两回事
- [2026-08-07 调试台「上传相册图片」改走真实投屏竖向链路 + 传输参数放开为输入框](changes/2026-08-07-调试台改走真实投屏竖向链路与传输参数.md) — 仅小程序**调试台**；选图→按广播ID判大小尺寸→设备物理分辨率竖向中心裁切→统一压缩→第三方抖动接口出帧→字节数铁闸→BLE 图传，与正式投屏同源但**不调设备管理/投屏记录接口**（不在真实环境留痕）；发包窗口改输入框默认 50、新增 0x21 数据字节(489)与 MTU(500) 参数，改完立即生效；删除已作废的「上传转换参数」整块。真实投屏默认值（窗口 10 / 每包 236 / MTU 247）一字未动，由 `tests/debug-transfer-params.test.js` 锁住
- [2026-08-07 SSE 接入文档核对（「-改」版）：进度补间 + 一键生图即时上屏](changes/2026-08-07-SSE文档核对与进度补间.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-07-AI流式SSE与JWT补齐.md`；App 侧一次性追平 07-29 的 JWT 头 + 08-06 的 SSE + 本轮改动，⚠️ 其中 `Authentication` 头此前**从未发送过**，AI 请求本会全线 403，只是入口屏蔽着没炸出来）；协议本身没变（解析层一行未动），新增可选 `model_type`；后端只发里程碑改由前端每 80ms 补间出中间读数（demo 图上的 47%），进度文案改成认 `stage`（拆出 85「正在下载图片…」）；一键生图改成「气泡先上屏、再建会话」，消掉点下去干等约 1s 的空窗；纠正「`image` 约 50% 到」的旧理解（实际排在 `progress 90` 之后）
- [2026-08-06 AI 改流式（SSE）对接：预描述回复 + 生成进度条](changes/2026-08-06-AI流式对接与生成进度条.md) — 仅小程序（Flutter 按要求暂不同步）；地址切到 `boltstagent-web-…`，`/chat` 用 `enableChunked` + `onChunkReceived` 收流，`pre_text` 预描述秒回、`event.progress` 驱动 0→100 进度条、图约 50% 先上屏、文字边收边打；请求体与鉴权一字未改，老基础库降级回非流式
- [2026-08-06 支付体系（Token）与官方图库接入](changes/2026-08-06-支付体系与官方图库接入.md) — 仅小程序；新增 `subpackages/token`（管理/记录/确认购买）与 `subpackages/gallery`（图库/详情/收藏）两个分包，底栏扩到四格，「我的」页改单列列表并新增两个入口。⚠️ 首版两个模块的后端接口均不存在、数据由 mock 提供；Token 侧已于 2026-08-08 切到真实后端（见上），图库/收藏仍待接入
- [2026-08-06 「内容不够一屏却有滚动条」修复（滚动内容块的外边距合并）](changes/2026-08-06-滚动内容块假滚动条修复.md) — 仅小程序；`.fold-scroll-body` / `.binding-content` 补 `display: flow-root`，首个子元素的 `margin-top` 不再把「一屏高」的内容块整体下推（登录页多滚 376rpx，与折叠与否无关；Flutter 侧核对无此问题）
- [2026-08-06 登录页协议勾选行贴左修复（`align-self` 在块级父容器里失效）](changes/2026-08-06-登录页协议勾选行贴左修复.md) — 仅小程序；内容搬进 `.fold-scroll-body`（块级）后 `align-self: center` 失效，改用 `margin: … auto`（与折叠与否无关，所有机型都偏）
- [2026-08-05 设置类页面贴底按钮「看得见、点不动」修复（退出登录 / 用户注销）](changes/2026-08-05-设置页贴底按钮点不动修复.md) — 仅小程序；`.fold-scroll(z-index:1)` 盖住了缺省层级的贴底按钮，补 `z-index: 2`（Flutter 侧核对无此问题）
- [2026-08-05 投屏预览页折叠屏「页面像被锁住」修复 +「我的相册」默认选中连接中的设备](changes/2026-08-05-预览页折叠屏修复与我的相册默认选中连接设备.md) — 折叠屏一项仅小程序（修正同日上一份文档里投屏预览页那一格的做法，其余页面方案不变）；「默认选中连接中的设备」**双端已同步**（Flutter 2026-08-11 核对：规则本就在，但判据是缓存标记，已改为现算真实 BLE 会话）
- [2026-08-05 「我的相册」张数改为投屏成功记录数 + 折叠屏适配放宽到全部页面](changes/2026-08-05-我的相册计数与全站折叠屏适配.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-05-我的相册计数与折叠屏方案.md`）；折叠屏公共方案落在 `styles/fold-adapt.wxss` + `utils/fold-adapt.js`
- [2026-08-04 广播 Device_ID 由 4 字节扩到 6 字节：搜索页设备ID 与详情页对齐](changes/2026-08-04-广播设备ID扩到6字节.md) — **双端已同步**；⚠️ 客户端必须先于固件灰度上线
- [2026-08-04 竖向构图改按设备 `verticalRotation` 旋转（缺省不旋转）](changes/2026-08-04-竖向旋转改由verticalRotation决定.md) — **双端已同步**；取代「竖向固定 180°」口径
- [2026-08-04 首页折叠屏滚动适配 +「我的相册」合并「设备照片」与「投屏管理」](changes/2026-08-04-折叠屏滚动适配与我的相册合并.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-04-我的相册合并与折叠屏核对.md`；折叠屏一项 App 核对后无需改动）
- [2026-08-04 小程序文案、多图首图刷新与竖向旋转](changes/2026-08-04-小程序电子纸设备文案与多图首图刷新.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-11-文案口径统一与多图首图刷新.md`）；短 UI 标签「设备」/ 完整提示语「电子纸设备·e-paper device·電子ペーパー」、称谓「您」四语种同改，多图投屏收尾改刷**首张**槽位。⚠️ 清单里的「竖向固定 180°」已被同日 `verticalRotation` 变更取代，两端都不再执行；「一键清空」右侧说明疑似漏字「空电子纸设备…」待产品定夺
- [2026-08-03 结果页底部按钮间距 + 抖动接口 `-118` 连接超时定位](changes/2026-08-03-结果页按钮间距与抖动接口连接超时.md) — 仅小程序，Flutter 未同步
- [2026-08-02 选图上限 10 张、预览切图闪回、命名弹窗「稍后」、容量行 `已用/上限`](changes/2026-08-02-选图上限与预览切图四项.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-02-选图上限与预览切图四项.md`）
- [2026-08-02 导航栏设备下拉：与菜单同宽、整体居中、让开微信胶囊](changes/2026-08-02-导航栏设备下拉居中与同宽.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-02-导航栏设备下拉居中与同宽.md`）
- [2026-08-01 交互与文案十一项优化](changes/2026-08-01-交互与文案十一项优化.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-08/2026-08-01-交互与文案十一项优化.md`）
- [2026-07-31 设备与照片交互优化阶段记录](changes/2026-07-31-设备与照片交互优化阶段记录.md) — **双端已同步**
- [2026-07-31 设备动态旋转角与 AI 视觉补全](changes/2026-07-31-设备旋转角与AI视觉补全.md)
- [2026-07-30 设备身份登记表（未连接设备被误报「请删除后重新绑定」）](changes/2026-07-30-设备身份登记表.md) — **双端已同步**（Flutter 见 `flutter/docs/history/2026-07/2026-07-30-FBP连接优化与身份登记表.md`）
- [2026-07-29 AI 接口 JWT 鉴权请求头](changes/2026-07-29-AI接口JWT鉴权头.md)
- [2026-07-29 图库设备筛选从设备名改为设备 ID](changes/2026-07-29-图库设备筛选按设备ID.md)
- [2026-07-29 投屏管理 Tab 即时切换与列表局部 loading](changes/2026-07-29-投屏管理Tab即时切换.md)
- [2026-07-29 蓝牙搜索改「边搜边显示」+ AI 发送去重 + 详情页分辨率](changes/2026-07-29-蓝牙边搜边显示与AI发送去重.md)
- [2026-07-28 AI 网关错误提示与服务协议 v2](changes/2026-07-28-AI网关错误与服务协议v2.md)
- [2026-07-28 AI 服务协议与按用户授权](changes/2026-07-28-AI服务协议.md)
- [2026-07-22 照片预览需求调整](changes/2026-07-22-照片预览需求调整.md)
- [2026-07-24 AI 模块开发进度](changes/2026-07-24-AI模块开发进度.md)

新重要变更使用：

```text
docs/changes/YYYY-MM-DD-topic.md
```

建议包含以下章节：

```markdown
# Change Title

## Background
## Problem
## Solution
## Affected Areas
## Technical Decisions
## Risks
## Follow-up
```

## 归档

[archive/](archive/README.md) 保存旧方案、排障过程、性能实验、审查清单、接入进度和跨仓库交接。归档文档只回答历史问题，不能作为当前实现的权威来源。

## CodeGraph 与 Markdown

### CodeGraph：当前代码真相

用于回答：

- 文件、函数和常量当前在哪里；
- 谁调用谁、改动影响哪些代码；
- 当前入口、调用链、数据流和测试候选；
- 重构前后的依赖变化。

不要在 Markdown 中长期手工维护可由 CodeGraph 实时推导的完整函数、调用者和行号清单。

### Markdown：长期项目记忆

用于回答：

- 为什么这样设计；
- 产品和业务口径是什么；
- API、BLE、OTA 等外部约束是什么；
- 哪些方案被否决以及原因；
- 重要 Bug、性能、安全和一致性风险；
- 真机验证结果、跨端交接和后续工作。

## 维护流程

### 修改前

1. 运行 `codegraph status`。
2. 用 `codegraph explore "<问题、文件或符号>"` 理解当前实现和影响面。
3. 从本页找到该领域唯一的长期主文档。

### 修改后

1. 运行相关测试、静态检查和必要的真机验证。
2. 代码结构变化时运行：

   ```powershell
   codegraph sync
   codegraph status
   ```

3. 架构、协议、API、安全、性能或重要 Bug 变化时更新长期主文档，并创建日期型变更记录。
4. 纯重命名或等价重构通常只需同步 CodeGraph，删除文档中的过期符号引用。

## 文档生命周期

新文档在标题后加入：

```markdown
> 状态：current | draft | planned | superseded | archived
> 负责人：姓名或团队
> 最后核对：YYYY-MM-DD
> 适用范围：小程序 | Flutter | 后端 | 固件
> 权威来源：链接或文档路径
> 取代/被取代：无或文档路径
```

冲突处理顺序：

1. 上游当前 API/协议/固件规范；
2. 当前源码和已同步 CodeGraph；
3. 标记为 `current` 的架构、协议和决策文档；
4. 日期型变更记录；
5. archive 中的历史快照。

实现与批准的需求或外部契约不一致时，不静默修改文档迁就代码；应明确决定修改实现还是更新决策。

## 定期维护

- 每个重要变更：探索影响、测试、同步 CodeGraph、更新主文档和变更记录。
- 每两周：核对待真机验证、待后端确认和跨端同步事项。
- 每个发布：复核接口、设备协议、权限、安全和数据一致性。
- 每季度：检查断链、乱码、截断、重复主文档和长期未核对的 `current` 文档。
