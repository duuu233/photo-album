# 图片传输性能优化计划

## 决策记录

- 决策日期：2026-07-10
- 当前状态：A+B 已实施；第二阶段 D1/D2/F1 已实施（见下方「第二阶段实施记录」）；均待真机测试
- 第一阶段：执行 A（性能测量）和 B（小程序低风险优化）
- 第二阶段（2026-07-10 决策）：执行 D1（预组包+查表 CRC16）、D2（查表 CRC32 移预取）、F1（首张转码与连接并行）
- 后续安排：完成真机测试并取得基线/优化后数据，再决定是否执行 C、D、E
- 本阶段不修改图片格式、BLE 协议或固件

## 实施记录

2026-07-10 已完成：

- 增加预览处理、连接、设备信息、连接间隔、上传转码、帧下载/读取、BLE 起止应答、数据吞吐、重传和记账耗时统计。
- `getBLEMTU` 改为按 `writeNoResponse` 查询。
- 新增仅执行 0x01 的投屏核心信息读取，0x03 固件版本不再阻塞首张投屏。
- 第一张网络准备与连接间隔优化并行。
- 投屏记录更新改为并行任务，整单结束统一收敛，不阻塞下一张 BLE。
- 正式投屏进度更新限制为约 120ms 一次；开始、完成和重试状态仍立即更新。
- 最后一包进入 ACK 等待前和超时后均复查确认位置，避免已完成后多等或误重发。

已完成的非真机验证：

- JavaScript 语法检查通过。
- `git diff --check` 通过。
- 内存 BLE 模拟完整跑通 0x20 → 0x21/0x23 → 0x22。
- 模拟“尾包 ACK 先于写回调到达”时无 600ms 额外等待。
- 模拟中间数据包丢失时可按累计 ACK 重发并恢复。
- 模拟 `setBLEMTU` 不可用时，确认 `getBLEMTU(writeNoResponse)` 返回 236 字节图片载荷。

尚未完成：真实 Android/iOS 手机和 3.7/5.89 寸设备上的基线、耗时与稳定性对比。

## 第二阶段实施记录（D1 / D2 / F1）

实施日期：2026-07-10。协议字节与 v1.5 完全一致，不改图片格式、BLE 协议、窗口/pace 调速逻辑（遵守 B6）。

### D2. 整图 CRC32 查表化并移入预取

- `utils/image-codec.js`：`crc32Mpeg2` 由逐位改为查表实现（新增模块内常量 `CRC32_MPEG2_TABLE`）。
  多项式 0x04C11DB7、初值 0xFFFFFFFF、不反转不异或均不变，结果与旧实现完全一致。
- 实测（桌面 Node，仅供参考量级）：5.89 寸一帧 326KB 由 23ms → 4ms；低端手机预计收益更大。
- CRC32 计算时机从「uploadImage 内、0x20 发出前」提前到预取阶段（见 D1 的 `prepareImageTransfer`）。

### D1. 0x21 数据帧预组包 + CRC16 查表化

- `utils/frame-protocol.js`：
  - `crc16Modbus` 改查表实现（新增 `CRC16_MODBUS_TABLE`），多项式 0xA001/初值 0xFFFF/字节序不变；
  - 新增 `buildImgDataFrame(data, seq, chunkSize)`：用 Uint8Array 整块 set + 查表 CRC16 组一帧完整
    0x21 帧，与旧路径 `buildFrame(CMD.IMG_DATA, buildImgDataPayload(...))` 逐字节等价（已验证）。
- `utils/device-ble.js`：
  - 新增导出 `prepareImageTransfer(deviceId, data)`：整图 CRC32 + 按会话 dataChunk 预组全部 0x21 帧；
    每 256 帧 yield 一次，避免饿死并行进行中的上一张图传 ACK 处理；会话未就绪时只算 CRC32（frames=null）。
  - `uploadImage` 新增可选 `options.prepared`：dataSize 对得上才用其 CRC32；chunkSize/帧数对得上才用
    预组帧；任一不符自动回退现算现组（调试页等旧调用方不传 prepared，行为不变）。
  - `writePacket` 改为接收已组好的整帧 ArrayBuffer；原 `writeFrame` 辅助函数删除（仅它在用）。
  - 实测（桌面 Node）：1384 包组帧 57ms → 21ms，且全部移出「两次 BLE 写之间」的串行热路径。
- 新增 stats 字段（单张汇总日志）：`crcPrecomputed`（CRC32 是否预取算好）、`prebuiltFrames`（是否用了预组帧）。
  真机排查时先看这两个标志：均应为 true；为 false 说明走了回退路径（首张早预取时会话未就绪属正常）。

### F1. 首张转码/下载与「连接 → 0x01」并行

- `subpackages/projection/result/result.js`：
  - 预取流水线（framePrefetch/startPrefetch）从「0x01 之后」提前到连接之前声明；`startPrefetch(idx, sizeInfo)`
    显式传尺寸：首张早启动用设备记录尺寸，其余张用 0x01 真实尺寸。
  - 早启动条件：设备记录带后端按型号下发的 width/height（`guessedSize`），或首张是再次投屏 imgBle
    直传（`firstDirect`，完全不依赖尺寸）。满足即在 `ensureConnection` 前启动第一张的转码+下载。
  - 0x01 返回后校验：`guessedSize` 与真实 info 宽高不一致 → 作废首张预取（`framePrefetch[0]=null`）
    按真实尺寸重取，日志 `[投屏] 设备记录尺寸(…)与设备实际(…)不一致`；原有「帧字节数=宽×高÷2」
    校验保留，错帧绝不会发往设备。
  - `acquireFrame` 两条链路（正常转码/imgBle 直传）在下载完成后统一调 `prepareTransfer`（内部调
    `deviceBle.prepareImageTransfer`），把 D1/D2 的预处理并入预取；失败只 console.warn，不阻断本张。
  - 整单汇总新增：`firstPrefetchEarly`（首张是否早启动）、`firstPrefetchDiscarded`（是否因尺寸不符作废）；
    单张 timings 新增 `prepareMs`。
- 预期收益：首张开始 BLE 前等待由「(连接+0x01) + (转码+下载)」串行变为两者取大，冷连接场景省 1~3s。
- 已知代价：早启动的转码会先建一条 deviceUploadState=0 的投屏记录，若整单在图传开始前失败/中止，
  该记录与「转码成功但 BLE 失败」场景一致（可重传），非新增问题；尺寸不符作废时会多一次转码调用（极少发生）。

### 回滚指引

- 三项相互独立，可按文件粒度部分回滚：
  - 仅回滚 F1：还原 `result.js`（预取流水线退回 0x01 之后启动、acquireFrame 去掉 prepareTransfer）；
    device-ble/frame-protocol/image-codec 可保留，D1/D2 自动退化为「uploadImage 内现算现组（仍查表加速）」。
  - 仅回滚 D1 预组包：删 `prepareImageTransfer`/`buildAllImgDataFrames`、`uploadImage` 的 prepared 分支、
    result.js 的 prepareTransfer；`writePacket`/`buildImgDataFrame` 可保留（等价实现）。
  - 仅回滚查表 CRC（怀疑真机 CRC 不一致时优先查这里）：把 `crc16Modbus`/`crc32Mpeg2` 换回逐位实现即可，
    两者已做等价性验证，理论上不存在差异。
- 定位问题线索：
  - 设备回 0x0A「校验失败(整图CRC32不符)」→ 查 D2（prepared.crc32 是否配错图，uploadImage 已按 dataSize 拦截）；
  - 设备回 0x03「CRC 错误」（单帧）→ 查 D1 `buildImgDataFrame`/查表 CRC16；
  - 首张投错尺寸/「后端返回的不是设备要的六色4bpp帧」→ 查 F1（guessedSize 来源 device.width/height 是否可信）；
  - 单张日志 `prebuiltFrames=false` 且非首张 → prepared 帧数/分包对不上（MTU 变化、prepared 配错图）。

## 首份真机数据与追加优化（2026-07-10）

### 首份真机 trace 结论（iPhone 12 / iOS 26.5 / 3.7 寸 / 单张 / 成功，traceId=mreb7myc-g97p5x）

- 整单 25.3s = 网络准备 5.7s（后端转码 3.9s + 下载 1.6s）+ BLE 数据 15.2s + 墨水屏刷新 4.1s + 其他 0.3s。
- D1/D2/F1 均生效：crcPrecomputed/prebuiltFrames=true、crcMs=0、prepareMs=197（已在预取）；
  F1 重叠 282ms（连接是预热复用的 connectMs=7，无冷连接可省）。
- 写路径已不是瓶颈：writeTotalMs 842ms 仅占 dataMs 5.5%；pace=0 全程 0 重试 0 丢包
  （D3 在此机型成立；D4 写流水线无收益，废弃）。
- **新瓶颈**：ackWaitMs 14.1s（占 dataMs 93%），每 10 包一个 ~205ms 的 ACK 周期 = 11.5KB/s。
  ① 固件每 10 包才回 0x23 且窗口=10，滑动窗口退化为批量停等（要固件提高 ACK 频率/加大窗口，计划 E）；
  ② 优化前链路 45ms、请求 7.5ms 设备回 OK——但 iOS 常拒绝 <15ms 的参数更新请求，链路疑似仍跑旧值
  （由 205ms/窗 倒推 7.5ms 必未生效）→ 由下方探针求证。

### 追加 2：连接间隔 0x05 回读探针

- `utils/device-ble.js` `optimizeConnectionIntervalForTransfer`：0x13 设置后 sleep 300ms（等参数更新
  协商落定）再回读 0x05，返回值新增 `requested`（请求值）/`verified`（回读成功）/`applied`（回读值=目标，
  真实生效）；`current` 一律为回读到的实际值（回读失败回落请求值）。回读失败不影响图传。
- `result.js`：`changed && verified && !applied` 时打 console.warn「连接间隔未真实生效：请求 Xms，
  链路实际 Yms」。+300ms 探针耗时被并行的首张网络准备吸收，不上关键路径。
- 用途：真机跑一单即可确认 iOS 实际给了多少；若确认 7.5 被拒，调试页把 transferConnIntervalMs
  同步成 15ms 对照（零代码 A/B）。
- 回滚：还原该函数为「set 后直接返回」即可；返回值旧字段（changed/previous/current）语义兼容。

### 追加 3：最后一张 0x24 刷屏不阻塞成功页

- `result.js` 主循环：0x24 由 `await` 改为 fire-and-forget（.then/.catch 异步补记
  `phases.refreshScreenMs` / `refreshScreenError` 并单独打「[投屏性能] 刷屏完成(异步)」日志；
  整单汇总先打出、不含此项）。图片已全部写入设备后才发 0x24，投屏成败判定不受影响（此前刷屏失败
  本就被吞掉）。真机实测可让成功页提前 ~4.1s 出现。
- 已知代价：成功页出现时设备可能仍在刷屏，立刻「继续投屏」可能撞上设备忙(0x0B)/暂时断连——
  已有归类提示兜底（「当前设备繁忙，请稍后重试」/「设备未连接…」），且该场景在旧版「传完立即再投」
  时本就存在。若真机反馈此代价不可接受，回滚为 `await` 原样三行即可。
- 验证（内存 BLE 模拟追加）：探针三态（请求被拒 verified=true/applied=false、请求被批准 applied=true、
  已在目标值 changed=false）全部通过；全部改动文件 node --check 通过。尚未真机验证。

### 追加 4：连接间隔平台默认值（安卓 7.5ms / iOS 15ms）

- 依据：第二份真机 trace（iPhone 12，调试页同步 15ms，traceId=mrec71sr-8mtqi3）——
  dataMs 15176→9600ms（-37%）、吞吐 11.12→17.58KB/s、每 10 包窗口 205→130ms，仍 0 重试 0 丢包；
  整单 25.3s→19.6s。证实 iOS 拒绝 7.5ms 请求、接受 15ms。
- `utils/device-ble.js`：固定默认值 `TRANSFER_CONN_INTERVAL_MS=7.5` 替换为按平台取值的
  `defaultTransferConnIntervalMs()`——android/ohos(鸿蒙) → 7.5ms；ios 及未知平台（探测失败、
  桌面端）→ 15ms 保守值（各平台都接受，最多慢一点；反之 iOS 请求 7.5 会停在更慢的 30/45ms）。
  `getTransferConnIntervalMs()` 改用它兜底；**调试页「同步投屏」写入的存储值仍优先于平台默认**。
- 注意：已经在调试页同步过间隔的手机（存储键 transferConnIntervalMs 有值）不受平台默认影响；
  要验证平台默认需先清掉该存储值。
- 回滚：还原 `defaultTransferConnIntervalMs` 为常量 7.5 即可。
- 验证：模拟平台切换（ios/android/ohos/devtools）默认值选择、不传 ms 时 optimize 按平台默认请求，
  全部通过；node --check 通过。安卓真机基线仍缺。

### 第二阶段验证（非真机）

- Node 等价性测试：查表 CRC16/CRC32 与旧逐位实现在 0~326400 字节 8 组数据上完全一致；
  `buildImgDataFrame` 与旧 `buildFrame+buildImgDataPayload` 逐字节一致（含末包截短、chunk 174/236）。
- 内存 BLE 模拟（0x20 → 0x21/0x23 → 0x22，含丢包重传）：prepared 路径、无 prepared 回退路径、
  prepared 错配自动回退路径均整包校验通过，设备收到字节与源数据一致。
- 全部改动文件 `node --check` 语法通过。
- 尚未完成：真机（Android/iOS × 3.7/5.89 寸）耗时对比与稳定性验证；重点观察首张
  `connectMs/readTransferInfoMs` 与 `acquireWaitMs` 的重叠效果、`crcPrecomputed/prebuiltFrames` 命中率。

## A：性能测量

为每次投屏生成一次 trace，按整单和单张图片记录以下数据：

### 环境信息

- 手机平台、系统版本、微信版本和小程序基础库版本
- 设备型号、屏幕类型、固件版本
- 网络类型
- 协商 MTU、图片分包大小、窗口大小和配置 pace

### 阶段耗时

- 预览页自动裁剪和临时文件导出
- BLE 连接或已有连接复用
- 读取设备核心信息（0x01）
- 读取/设置连接间隔（0x05/0x13）
- 原图上传及服务器转换总耗时
- frame.bin 下载和本地读取
- CRC32 计算
- IMG_START（0x20）应答
- IMG_DATA（0x21）数据发送
- IMG_END（0x22）校验和落盘
- 投屏记录更新
- 最终刷新屏幕（0x24）

### BLE 统计

- 原始字节数、总包数、已确认包数和实际数据吞吐（KB/s）
- 写入调用耗时、ACK 推进次数和 ACK 等待耗时
- 重试次数、重发包数、最终实际 pace
- 成功、失败和中断原因

日志只记录性能与协议元数据，不记录图片内容、完整图片地址或用户隐私数据。每张图片结束时输出一条汇总，整单结束时再输出一条总汇总，避免逐包日志本身影响传输性能。

## B：小程序低风险优化

### B1. 修正 MTU 查询方式

`getBLEMTU` 按实际数据写入方式使用 `writeNoResponse`，避免部分设备按错误写入类型查询失败后回退到较小分包。

### B2. 缩短首张关键路径

- 投屏只同步读取 0x01 所需的屏幕尺寸、类型、容量和图片掩码。
- 固件版本 0x03 移出首张关键路径，改为后台读取或复用已有信息。
- 0x01 返回后立即启动第一张的上传、转码和下载，同时执行连接间隔优化。
- 保留现有预览页 BLE 连接预热和结果页连接复用。

### B3. 后端记账移出图片间串行路径

设备完成一张图片后，投屏记录更新不再阻塞下一张 BLE 发送。记账任务仍需保证最终执行，并保留成功/失败日志；记账失败不能改变设备已传成功的事实。

### B4. 限制进度 UI 更新频率

BLE ACK 继续实时参与流控，但页面进度最多约每 100~200ms 或百分比变化时更新一次，避免高频 `setData` 占用 JS/UI 通道。开始、完成、重试和错误状态必须立即更新。

### B5. 修正尾包 ACK 竞争窗口

最后一轮发送后，在进入 600ms ACK 等待前再次检查最后包是否已经确认；ACK 等待超时后也先复查确认位置，再决定是否重发，避免已完成却额外等待或重发。

### B6. 暂不继续激进调速

第一阶段不提高 10 包窗口上限，也不强制把 pace 固定为 0。先通过 A 记录写入耗时、ACK 延迟和重传成本，再决定是否调整调速算法。

## 验收与测试

至少覆盖以下组合：

- Android 和 iOS
- 3.7 寸和 5.89 寸设备
- 单张和连续 5 张
- 首次投屏和再次投屏
- Wi-Fi 和移动网络（网络阶段对比）
- 已连接复用和冷连接

对比指标：

- 首张开始 BLE 发送前耗时
- 单张端到端耗时
- BLE 纯数据段耗时和 KB/s
- 5 张整单耗时
- 重传率、失败率和尾包额外等待次数
- 投屏记录最终一致性

先取得修改前基线，再使用同一组图片、设备和手机复测修改后版本。只有成功率与记录一致性不下降时，性能优化才算通过。

## 暂缓方案

完成 A+B 真机测试后，按瓶颈决定是否继续：

- C：若图片准备/上行占比最高，改为上传原图和裁剪/旋转参数，由后端完成目标尺寸处理。
- D：若重复下载或批量等待明显，增加 frame.bin 本地缓存，并评估有界并发预取。
- E：若 BLE 数据段占比最高，与固件协作增加六色 3bpp 格式或流式无损压缩，并评估更大的设备缓冲、DLE/2M PHY。
