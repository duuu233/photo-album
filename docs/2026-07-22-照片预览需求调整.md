# 2026-07-22 · 照片预览页需求调整（竖向/横向取景 + 常驻编辑 + 按钮切图）

> 🛠 **维护约定**：每次修改本需求相关的问题/功能，务必在下方「操作日志」补一条（日期 + 改了什么 + 关联文件/commit），**最新在上**。
>
> 关联：`subpackages/projection/preview/preview.{js,wxml,wxss}`；`docs/照片预览-编辑交互.md`（旧交互演进史）；
> memory `projection-upload-must-be-device-resolution`（竖向导出必须设备物理分辨率；07-22「调整三」起横向为对调分辨率直出）。
>
> ⚠️ **本次只改小程序**。Flutter APP 暂不动，等用户通知后再同步（见文末「APP 同步待办」）。

## 操作日志（最新在上）

- **2026-07-22（调整三）**：**去掉横向导出的 270° 旋转**（用户拍板）——横向取景改为**按可视区域所见直出**：
  导出画布 = 取景方向分辨率（竖=设备物理分辨率、横=宽高对调，如 480×720 设备横向出 720×480），
  两方向都不再做任何旋转，可视区域裁剪图**原样上传接口**，横向图的方向适配交给接口侧。
  - 连带删除：`EXPORT_ROTATE_DEG/RAD`、`PREVIEW_COUNTER_ROTATE_DEG`、`computeRotatedImgStyle`、
    图片 `imgStyle` 字段（wxml 的反向转正 style 一并删）。烘焙几何仅改「画布尺寸与 k 的取法」，
    平移/缩放/用户旋转的映射一行未动。
  - ⚠️ **风险备忘（真机第一优先验证）**：07-17 实锤过后端按「上传图像素」出帧且忽略
    targetWidth/Height；若接口侧未适配横向图，设备按竖向行宽解析会**无报错整幅花屏**（07-20 铁证）。
    复现花屏 → 先查接口侧；旧「导出竖 + 270°」方案从本文件 git 历史找回。
  - result.js 仍按设备（竖向）尺寸传 targetWidth/Height 未动；若后端开始启用该字段，横向图需另议。
  - 关联：`preview.{js,wxml}`。
- **2026-07-22（优化二）**：**去掉取景框全套视觉**——裁剪框边框/四角装饰/九宫格参考线/框外黑色半透明遮罩都删了，改为可视区域容器 `.edit-clip`（位置尺寸=frame，由 JS 下发）直接 `overflow:hidden` 裁掉区外内容，观感与 photo-wrap 圆角卡片一致；编辑层退化为铺满舞台的透明触摸面（手势范围不变）。交互全部保留（单指平移/双指缩放旋转/右上角转90°/横竖切换/投屏烘焙）。
  - 连带改动：图片定位从「相对整个编辑层」改为「相对 .edit-clip」（`enterEdit`/`setOrientation` 的 `editImgLeft/Top` 不再加 `frame.left/top`）；tx/ty 仍相对可视区中心，**夹取与导出几何一行未动**。
  - 删除：wxml 的 `.edit-shade/.edit-frame/.crop-third×4/.edit-corner×4`；wxss 同名样式与编辑层深色底。
  - 关联：`preview.{js,wxml,wxss}`。
- **2026-07-22**：按下方需求清单完成小程序侧改造（preview.js/wxml/wxss 全量重写，详见「实现说明」）。未真机验证。

## 需求清单（用户原话归纳）

1. **去掉「裁剪」「旋转」按钮**（底部工具栏原有三个，只留「原图」）。
2. 新增**「竖向 / 横向」按钮**，放在图片下面，交互效果参考 element-plus 的 **el-segmented 分段控制器**（滑块滑动切换）：
   - **竖向** = 按当前设备比例显示图片（原来不旋转图片、按设备尺寸展示的样子）；
   - **横向** = 把设备宽度当高度（宽高对调），按设备比例等比展示——相当于裁剪，可视区域就是取景框，
     看不到的部分**用手拖动调整**；
   - 横竖切换**改变的只是可视区域**，图片本身不动。
3. **默认即可编辑**：图片直接支持双指缩放 & 双指旋转（无需先点某个按钮进入编辑态）。
4. **去掉「保存」按钮**。
5. **点「开始投屏」自动保存当前效果**——按最终呈现（取景框内所见）出图上传给接口，这一步才真正改图。
6. 图**右上角悬浮一个「转90°」按钮**（顺时针）。
7. **左右滑动切图的交互去掉**，在原「裁剪/旋转/原图」工具栏的下面放**「上一张 / 下一张」按钮**。

## 实现说明

### 交互模型：常驻编辑态 + 非破坏性状态

- 进入页面（`onReady`）/ 点「上一张/下一张」都会自动 `enterEdit()`：固定取景框 + 图片在框下自由
  单指平移、双指缩放+旋转（原「长按旋转/双指旋转」二选一的对比开关**已删除**，定版双指旋转）。
- **每张图一份编辑状态**（`this._states[index]` = `_edit` 快照：方向 + tx/ty/zoom/angle + 几何量），
  切图时保存、切回时恢复；**不落文件**。点「开始投屏」才统一出图（`prepareProjectionImages`）：
  - 状态真的动过（缩放/平移/旋转/切了横向）→ `bakeEditedImage` 按框内所见烘焙为设备物理分辨率新图；
  - 什么都没动（`isPristineEdit`）→ 等价未编辑：`cropW>0`/`imgBle` 原样保留（**imgBle 直传判定不被破坏**），
    其余走 `coverCropOne` 竖向中心裁切。
- 换图不再用 swiper：wxml 里 swiper 已删，改单编辑层 + 「上一张/下一张」按钮（多图才显示，端点置灰）。

### 竖向/横向（取代 07-20 的 VIEW_SWAPPED 总开关）

- `ORIENT_PORTRAIT / ORIENT_LANDSCAPE`，**按张记录**，默认 `DEFAULT_ORIENTATION = 竖向`
  （= 07-20 改版前的原始样子；若产品要默认横向，改这一个常量即可）。
- 切换只重算取景框与 cover 基准，并用 zoom 反向补偿保住图片的绝对显示大小/位置（`setOrientation`），
  夹取逻辑（`_clampTransform`）会在新框盖不满时自动把 zoom 顶上去——即「横向类似裁剪，超出部分拖动可见」。
- **导出（2026-07-22「调整三」起）**：画布 = 取景方向的可视区域分辨率，**无任何旋转**、所见即所得。
  - 竖向取景：设备物理分辨率（如 480×720），与旧行为一致；
  - 横向取景：宽高对调分辨率**直出**（如 720×480），裁剪图原样上传，方向适配由接口侧负责。
  - 旧方案（07-20 ~ 07-22 调整三前）：横向整幅转 270° 画进竖向画布 + 预览 `computeRotatedImgStyle`
    反向转正——已整链路删除，回滚从 git 历史找回。
  - ⚠️ 历史教训仍然有效：后端若按「上传图像素」出帧且不适配横向，设备按竖向行宽解析会无报错花屏
    （见顶部操作日志「调整三」风险备忘）。

### UI

- 分段控制器 `.orient-seg`：灰底容器 + 白色滑块（`transition: transform`）滑到选中项，选中文字橙色——仿 el-segmented。
- 右上角 `.rotate-fab`：跟随取景框右上角定位（JS 下发 px 坐标 + `translateX(-100%)`），`catchtap="rotate90"` 顺时针 +90°。
- 工具栏只剩「原图」（二次确认后清空该图全部编辑与已烘焙文件、恢复 `_origSrc`，并重新进入编辑态）。
- 「开始投屏」不再因编辑态置灰（编辑态是常驻的），仅投屏中防连点。
- 舞台高由 760rpx 收到 **680rpx**（多了分段器和切图按钮两行）；`STAGE_H_RPX` 已同步，两处改动必须成对。

### 删除清单

- 底部「裁剪/旋转」按钮及 `selectTool`、「保存」按钮及 `savePhoto`、swiper 及 `onSwiperChange`；
- 旋转模式对比开关（`rotateMode / setRotateMode / _armRotate / _clearLongPress / LONG_PRESS_*` 与对应 wxml/wxss）；
- `VIEW_SWAPPED` 总开关（被按张 orientation 取代）、`coverCropUnedited`（并入 `prepareProjectionImages`）；
- wxss 死代码：`.preview-save / .rotate-mode-switch / .rms-item / .compress-*`。

### 待真机验证

1. **横向直出上传后的设备成像（第一优先）**：接口侧是否适配横向图——花屏/方向不对即回滚 270° 方案（见操作日志「调整三」）；
2. 常驻编辑的手势跟手度、切图（enterEdit 重建）速度；
3. 多图分别设不同横竖向 + 部分不动，投屏后每张成像是否与预览一致；
4. iPad/长短屏下新增两行后的纵向布局是否溢出。

## APP 同步待办（等用户通知再做）

Flutter 端（/home/pg/dh/flutter）预览页仍是旧交互（裁剪/旋转/保存 + 滑动切图 + VIEW_SWAPPED 常亮横向）。
同步时把本文「需求清单 + 实现说明」整体移植，注意：导出分辨率铁律、270° 只用于横向、imgBle 直传判定三点不能走样。
